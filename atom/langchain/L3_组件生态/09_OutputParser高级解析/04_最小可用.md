# 最小可用知识

> 掌握以下内容，就能在 80% 的场景中正确使用 OutputParser

---

## 核心原则

**20% 的知识解决 80% 的问题**：
- 优先使用现代方法（`with_structured_output()`）
- 必要时回退到传统方法（`OutputParser`）
- 掌握基础错误处理
- 知道何时选择哪种方法

---

## 4.1 优先使用 with_structured_output()

**概念说明**：
现代 LLM（GPT-4.1+, Claude 3+）支持原生结构化输出，使用 `with_structured_output()` 是最简单、最快速的方法。

**代码示例**：
```python
from langchain_openai import ChatOpenAI
from pydantic import BaseModel, Field

class Person(BaseModel):
    """Information about a person."""
    name: str = Field(description="The person's name")
    age: int = Field(description="The person's age")

# 一行代码搞定
llm = ChatOpenAI(model="gpt-4.1")
structured_llm = llm.with_structured_output(Person, method="json_schema")

# 直接调用
result = structured_llm.invoke("Michael is 35 years old")
print(result)  # Person(name="Michael", age=35)
```

**实际应用场景**：
- 生产环境的数据提取（客户信息、订单数据）
- 表单自动填充
- 结构化问答系统

---

## 4.2 回退到 PydanticOutputParser

**概念说明**：
当模型不支持原生结构化输出时，使用 `PydanticOutputParser` 手动解析 LLM 输出。

**代码示例**：
```python
from langchain_core.output_parsers import PydanticOutputParser
from langchain_core.prompts import PromptTemplate

# 创建 Parser
parser = PydanticOutputParser(pydantic_object=Person)

# 获取格式指令（重要！）
format_instructions = parser.get_format_instructions()

# 创建 Prompt 模板
prompt = PromptTemplate(
    template="Extract person info.\n{format_instructions}\n\nText: {text}",
    input_variables=["text"],
    partial_variables={"format_instructions": format_instructions}
)

# 创建链
chain = prompt | llm | parser
result = chain.invoke({"text": "Alice is 30 years old"})
```

**实际应用场景**：
- 使用开源模型（Llama, Mistral）
- 需要自定义解析逻辑
- 需要额外验证或处理

---

## 4.3 基础错误处理

**概念说明**：
使用 `OutputFixingParser` 自动修复格式错误，提高系统鲁棒性。

**代码示例**：
```python
from langchain.output_parsers import OutputFixingParser, OutputParserException

# 创建自动修复 Parser
fixing_parser = OutputFixingParser.from_llm(parser=parser, llm=llm)

# 方式1：直接使用 fixing_parser（推荐）
chain = prompt | llm | fixing_parser
result = chain.invoke({"text": "..."})

# 方式2：手动错误处理
try:
    result = chain.invoke({"text": "..."})
except OutputParserException as e:
    print(f"解析失败: {e}")
    result = fixing_parser.parse(e.llm_output)
```

**实际应用场景**：
- 生产环境的错误恢复
- 处理不稳定的模型输出
- 提高系统可靠性

---

## 4.4 选择合适的方法

**概念说明**：
根据模型支持和需求选择合适的方法。

**代码示例**：
```python
def get_structured_output(model_name: str, schema: type[BaseModel], input: str):
    """根据模型选择合适的方法"""
    llm = ChatOpenAI(model=model_name)

    # 支持原生结构化输出的模型
    if model_name in ["gpt-4.1", "gpt-4", "claude-3-opus"]:
        structured_llm = llm.with_structured_output(schema, method="json_schema")
        return structured_llm.invoke(input)

    # 不支持的模型，回退到 OutputParser
    else:
        parser = PydanticOutputParser(pydantic_object=schema)
        fixing_parser = OutputFixingParser.from_llm(parser=parser, llm=llm)
        format_instructions = parser.get_format_instructions()
        prompt = f"{input}\n\n{format_instructions}"
        chain = llm | fixing_parser
        return chain.invoke(prompt)
```

**实际应用场景**：
- 多模型兼容的系统
- 需要灵活切换模型
- 成本优化（根据任务选择模型）

---

## 这些知识足以

掌握以上 4 个要点，你就能：
- 在 80% 的场景中正确使用结构化输出
- 根据模型选择合适的方法
- 处理常见的解析错误
- 构建生产级的结构化输出系统

**下一步学习**：
- 深入学习流式解析（`BaseCumulativeTransformOutputParser`）
- 探索自定义 OutputParser 开发
- 了解 OpenAI Tools 集成
