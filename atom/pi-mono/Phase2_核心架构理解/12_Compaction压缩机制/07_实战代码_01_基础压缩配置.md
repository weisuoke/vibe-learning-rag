# 实战代码 01：基础压缩配置

> **目标**：通过实际代码示例学习如何配置和监控 Compaction

---

## 概述

本文档提供完整可运行的 TypeScript 代码示例，展示如何：
1. 配置压缩参数
2. 监控 token 使用
3. 启用/禁用自动压缩
4. 根据项目大小调整阈值

**前置要求：**
- Node.js 18+
- TypeScript 5.0+
- Pi-mono 已安装

---

## 示例 1：基础配置文件

### 创建配置文件

```bash
# 创建项目配置目录
mkdir -p .pi

# 创建配置文件
cat > .pi/settings.json << 'EOF'
{
  "provider": "anthropic",
  "model": "claude-opus-4",
  "compaction": {
    "enabled": true,
    "reserveTokens": 16384,
    "keepRecentTokens": 20000
  }
}
EOF
```

### 配置说明

```typescript
interface CompactionConfig {
  // 是否启用自动压缩
  enabled: boolean;

  // 预留空间（tokens）
  // 用于：压缩提示词 + 用户消息 + Agent 响应
  reserveTokens: number;

  // 保留最近消息（tokens）
  // 压缩时保留最近的这些消息
  keepRecentTokens: number;
}

// 默认配置
const defaultConfig: CompactionConfig = {
  enabled: true,
  reserveTokens: 16384,    // 16K tokens
  keepRecentTokens: 20000, // 20K tokens
};
```

### 不同场景的配置

```typescript
// 场景 1：长对话项目（保留更多上下文）
const longConversationConfig = {
  enabled: true,
  reserveTokens: 20000,    // 20K tokens
  keepRecentTokens: 30000, // 30K tokens
};

// 场景 2：短对话项目（更激进的压缩）
const shortConversationConfig = {
  enabled: true,
  reserveTokens: 10000,    // 10K tokens
  keepRecentTokens: 15000, // 15K tokens
};

// 场景 3：成本优化（最小化 token 使用）
const costOptimizedConfig = {
  enabled: true,
  reserveTokens: 8192,     // 8K tokens
  keepRecentTokens: 10000, // 10K tokens
};

// 场景 4：质量优先（保留更多上下文）
const qualityFirstConfig = {
  enabled: true,
  reserveTokens: 32768,    // 32K tokens
  keepRecentTokens: 50000, // 50K tokens
};

// 场景 5：禁用自动压缩（手动管理）
const manualOnlyConfig = {
  enabled: false,
  reserveTokens: 16384,
  keepRecentTokens: 20000,
};
```

---

## 示例 2：监控 Token 使用

### 完整监控脚本

```typescript
/**
 * token-monitor.ts
 * 监控 Session 的 token 使用情况
 */

import * as fs from 'fs';
import * as path from 'path';

interface Message {
  role: string;
  content: string;
  usage?: {
    input_tokens: number;
    output_tokens: number;
  };
}

interface SessionInfo {
  sessionId: string;
  totalMessages: number;
  contextTokens: number;
  contextWindow: number;
  usagePercentage: number;
  willCompactSoon: boolean;
}

/**
 * 从 JSONL 文件读取 Session
 */
function loadSession(sessionPath: string): Message[] {
  const content = fs.readFileSync(sessionPath, 'utf-8');
  const lines = content.trim().split('\n');

  return lines.map(line => JSON.parse(line));
}

/**
 * 获取最后一条 assistant 消息的 usage
 */
function getLastAssistantUsage(messages: Message[]): {
  input_tokens: number;
  output_tokens: number;
} | undefined {
  for (let i = messages.length - 1; i >= 0; i--) {
    const message = messages[i];
    if (message.role === 'assistant' && message.usage) {
      return message.usage;
    }
  }
  return undefined;
}

/**
 * 估算消息的 token 数
 */
function estimateTokens(content: string): number {
  // 简单估算：1 token ≈ 4 字符
  return Math.ceil(content.length / 4);
}

/**
 * 估算上下文 token 数
 */
function estimateContextTokens(messages: Message[]): number {
  // 优先使用最后一条 assistant 消息的 usage
  const usage = getLastAssistantUsage(messages);
  if (usage) {
    return usage.input_tokens;
  }

  // 否则手动估算
  let totalTokens = 0;
  for (const message of messages) {
    totalTokens += estimateTokens(message.content || '');
  }

  // 加上系统提示词的估算（约 2K tokens）
  totalTokens += 2000;

  return totalTokens;
}

/**
 * 分析 Session 信息
 */
function analyzeSession(sessionPath: string): SessionInfo {
  const messages = loadSession(sessionPath);
  const contextTokens = estimateContextTokens(messages);

  // 假设使用 Claude Opus 4 (200K context window)
  const contextWindow = 200000;
  const reserveTokens = 16384;
  const threshold = contextWindow - reserveTokens;

  const usagePercentage = (contextTokens / contextWindow) * 100;
  const willCompactSoon = contextTokens > threshold;

  return {
    sessionId: path.basename(sessionPath, '.jsonl'),
    totalMessages: messages.length,
    contextTokens,
    contextWindow,
    usagePercentage,
    willCompactSoon,
  };
}

/**
 * 格式化输出
 */
function formatSessionInfo(info: SessionInfo): string {
  const bar = '='.repeat(50);
  const usageBar = '█'.repeat(Math.floor(info.usagePercentage / 2));
  const emptyBar = '░'.repeat(50 - Math.floor(info.usagePercentage / 2));

  return `
${bar}
Session: ${info.sessionId}
${bar}

Messages: ${info.totalMessages}
Context Tokens: ${info.contextTokens.toLocaleString()}
Context Window: ${info.contextWindow.toLocaleString()}
Usage: ${info.usagePercentage.toFixed(2)}%

Progress: [${usageBar}${emptyBar}] ${info.usagePercentage.toFixed(1)}%

Status: ${info.willCompactSoon ? '⚠️  Will compact soon' : '✅ Normal'}

${bar}
  `;
}

/**
 * 主函数
 */
function main() {
  // 获取 Session 文件路径
  const sessionDir = path.join(process.env.HOME || '', '.pi', 'sessions');
  const sessionFiles = fs.readdirSync(sessionDir)
    .filter(f => f.endsWith('.jsonl'))
    .map(f => path.join(sessionDir, f));

  if (sessionFiles.length === 0) {
    console.log('No sessions found');
    return;
  }

  // 分析最新的 Session
  const latestSession = sessionFiles[sessionFiles.length - 1];
  const info = analyzeSession(latestSession);

  console.log(formatSessionInfo(info));

  // 如果即将压缩，给出建议
  if (info.willCompactSoon) {
    console.log('💡 Suggestions:');
    console.log('  - Consider manually compacting: /compact');
    console.log('  - Or wait for auto-compaction to trigger');
    console.log('  - Check if you need to increase reserveTokens');
  }
}

// 运行
main();
```

### 使用监控脚本

```bash
# 1. 保存脚本
cat > token-monitor.ts << 'EOF'
[上面的代码]
EOF

# 2. 运行脚本
npx tsx token-monitor.ts

# 输出示例：
# ==================================================
# Session: abc123.jsonl
# ==================================================
#
# Messages: 150
# Context Tokens: 85,000
# Context Window: 200,000
# Usage: 42.50%
#
# Progress: [█████████████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░] 42.5%
#
# Status: ✅ Normal
#
# ==================================================
```

---

## 示例 3：实时监控 Extension

### 创建监控扩展

```typescript
/**
 * .pi/extensions/token-monitor.ts
 * 实时监控 token 使用情况
 */

import { Extension } from '@mariozechner/pi-agent-core';

export const tokenMonitorExtension: Extension = {
  name: 'token-monitor',
  description: '实时监控 token 使用情况',

  async onLoad(context) {
    console.log('✅ Token Monitor 已加载');

    let messageCount = 0;

    // 监听消息添加事件
    context.session.on('message_added', (message) => {
      messageCount++;

      // 每 10 条消息输出一次状态
      if (messageCount % 10 === 0) {
        logTokenUsage(context.session);
      }
    });

    // 监听压缩前事件
    context.session.on('session_before_compact', (event) => {
      console.log('\n⚠️  即将触发压缩');
      console.log(`当前 tokens: ${event.contextTokens.toLocaleString()}`);
      console.log(`阈值: ${(event.contextWindow - event.reserveTokens).toLocaleString()}`);
      console.log(`使用率: ${(event.contextTokens / event.contextWindow * 100).toFixed(2)}%`);
    });

    // 监听压缩后事件
    context.session.on('session_after_compact', () => {
      console.log('✅ 压缩完成');
      logTokenUsage(context.session);
    });
  },
};

/**
 * 输出 token 使用情况
 */
function logTokenUsage(session: any) {
  const messages = session.messages;
  const usage = getLastAssistantUsage(messages);

  if (!usage) {
    return;
  }

  const contextTokens = usage.input_tokens;
  const contextWindow = session.config.contextWindow || 200000;
  const usagePercentage = (contextTokens / contextWindow) * 100;

  console.log(`\n📊 Token Usage: ${contextTokens.toLocaleString()} / ${contextWindow.toLocaleString()} (${usagePercentage.toFixed(2)}%)`);
}

function getLastAssistantUsage(messages: any[]): any {
  for (let i = messages.length - 1; i >= 0; i--) {
    if (messages[i].role === 'assistant' && messages[i].usage) {
      return messages[i].usage;
    }
  }
  return undefined;
}
```

### 使用监控扩展

```bash
# 1. 创建扩展文件
mkdir -p .pi/extensions
cat > .pi/extensions/token-monitor.ts << 'EOF'
[上面的代码]
EOF

# 2. 重新加载扩展
pi
/reload

# 3. 进行对话，观察输出
# 每 10 条消息会输出一次 token 使用情况
# 压缩前后会输出详细信息
```

---

## 示例 4：动态调整配置

### 根据使用情况自动调整

```typescript
/**
 * .pi/extensions/adaptive-compaction.ts
 * 根据使用情况自动调整压缩配置
 */

import { Extension } from '@mariozechner/pi-agent-core';

export const adaptiveCompactionExtension: Extension = {
  name: 'adaptive-compaction',
  description: '根据使用情况自动调整压缩配置',

  async onLoad(context) {
    console.log('✅ Adaptive Compaction 已加载');

    let compactionCount = 0;
    let messagesSinceLastCompaction = 0;

    // 监听消息添加
    context.session.on('message_added', () => {
      messagesSinceLastCompaction++;
    });

    // 监听压缩前事件
    context.session.on('session_before_compact', (event) => {
      compactionCount++;

      console.log(`\n🔄 第 ${compactionCount} 次压缩`);
      console.log(`距离上次压缩: ${messagesSinceLastCompaction} 条消息`);

      // 分析压缩频率
      if (compactionCount > 1) {
        analyzeAndAdjust(messagesSinceLastCompaction, event);
      }

      messagesSinceLastCompaction = 0;
    });
  },
};

/**
 * 分析并调整配置
 */
function analyzeAndAdjust(messagesSinceLastCompaction: number, event: any) {
  // 如果压缩太频繁（< 50 条消息）
  if (messagesSinceLastCompaction < 50) {
    console.log('⚠️  压缩过于频繁，建议增大 reserveTokens');
    console.log(`   当前: ${event.reserveTokens}`);
    console.log(`   建议: ${event.reserveTokens * 1.5}`);
  }

  // 如果压缩不够频繁（> 500 条消息）
  if (messagesSinceLastCompaction > 500) {
    console.log('💡 压缩频率较低，可以减小 reserveTokens 以节省成本');
    console.log(`   当前: ${event.reserveTokens}`);
    console.log(`   建议: ${event.reserveTokens * 0.8}`);
  }

  // 分析平均消息长度
  const avgTokensPerMessage = event.contextTokens / event.messages.length;
  console.log(`📊 平均消息长度: ${avgTokensPerMessage.toFixed(0)} tokens`);

  if (avgTokensPerMessage > 1000) {
    console.log('💡 消息较长，建议增大 keepRecentTokens');
  } else if (avgTokensPerMessage < 200) {
    console.log('💡 消息较短，可以减小 keepRecentTokens');
  }
}
```

---

## 示例 5：压缩配置测试工具

### 测试不同配置的效果

```typescript
/**
 * test-compaction-config.ts
 * 测试不同压缩配置的效果
 */

interface TestConfig {
  name: string;
  reserveTokens: number;
  keepRecentTokens: number;
}

interface TestResult {
  config: TestConfig;
  compactionCount: number;
  avgMessagesPerCompaction: number;
  totalCost: number;
}

/**
 * 模拟对话并测试配置
 */
function simulateConversation(
  config: TestConfig,
  totalMessages: number,
  avgTokensPerMessage: number
): TestResult {
  const contextWindow = 200000;
  let currentTokens = 0;
  let compactionCount = 0;
  let messagesSinceLastCompaction = 0;

  for (let i = 0; i < totalMessages; i++) {
    // 添加消息
    currentTokens += avgTokensPerMessage;
    messagesSinceLastCompaction++;

    // 检查是否需要压缩
    if (currentTokens > contextWindow - config.reserveTokens) {
      compactionCount++;

      // 压缩后保留最近的消息
      currentTokens = config.keepRecentTokens;

      // 记录
      messagesSinceLastCompaction = 0;
    }
  }

  // 计算成本（假设每次压缩 $0.01）
  const totalCost = compactionCount * 0.01;

  return {
    config,
    compactionCount,
    avgMessagesPerCompaction: totalMessages / (compactionCount || 1),
    totalCost,
  };
}

/**
 * 测试多个配置
 */
function testConfigs() {
  const configs: TestConfig[] = [
    { name: '默认配置', reserveTokens: 16384, keepRecentTokens: 20000 },
    { name: '激进压缩', reserveTokens: 8192, keepRecentTokens: 10000 },
    { name: '保守压缩', reserveTokens: 32768, keepRecentTokens: 50000 },
    { name: '成本优化', reserveTokens: 10000, keepRecentTokens: 15000 },
  ];

  const totalMessages = 1000;
  const avgTokensPerMessage = 500;

  console.log('='.repeat(80));
  console.log(`测试场景: ${totalMessages} 条消息，平均 ${avgTokensPerMessage} tokens/消息`);
  console.log('='.repeat(80));
  console.log();

  const results: TestResult[] = [];

  for (const config of configs) {
    const result = simulateConversation(config, totalMessages, avgTokensPerMessage);
    results.push(result);

    console.log(`配置: ${config.name}`);
    console.log(`  reserveTokens: ${config.reserveTokens}`);
    console.log(`  keepRecentTokens: ${config.keepRecentTokens}`);
    console.log(`  压缩次数: ${result.compactionCount}`);
    console.log(`  平均消息数/压缩: ${result.avgMessagesPerCompaction.toFixed(0)}`);
    console.log(`  总成本: $${result.totalCost.toFixed(3)}`);
    console.log();
  }

  // 推荐最佳配置
  const bestConfig = results.reduce((best, current) => {
    // 平衡压缩次数和成本
    const bestScore = best.avgMessagesPerCompaction / best.totalCost;
    const currentScore = current.avgMessagesPerCompaction / current.totalCost;
    return currentScore > bestScore ? current : best;
  });

  console.log('='.repeat(80));
  console.log(`推荐配置: ${bestConfig.config.name}`);
  console.log('='.repeat(80));
}

// 运行测试
testConfigs();
```

### 运行测试

```bash
# 运行测试
npx tsx test-compaction-config.ts

# 输出示例：
# ================================================================================
# 测试场景: 1000 条消息，平均 500 tokens/消息
# ================================================================================
#
# 配置: 默认配置
#   reserveTokens: 16384
#   keepRecentTokens: 20000
#   压缩次数: 5
#   平均消息数/压缩: 200
#   总成本: $0.050
#
# 配置: 激进压缩
#   reserveTokens: 8192
#   keepRecentTokens: 10000
#   压缩次数: 10
#   平均消息数/压缩: 100
#   总成本: $0.100
#
# ...
#
# ================================================================================
# 推荐配置: 默认配置
# ================================================================================
```

---

## 总结

**本文档提供的代码示例：**

1. **配置文件**：不同场景的压缩配置
2. **监控脚本**：分析 Session 的 token 使用情况
3. **监控扩展**：实时监控 token 使用
4. **自适应扩展**：根据使用情况自动调整配置
5. **测试工具**：测试不同配置的效果

**关键要点：**
- 根据项目特点选择合适的配置
- 使用监控工具了解 token 使用情况
- 根据实际使用调整配置参数
- 平衡压缩频率和成本

---

**下一步学习：**
- **实战代码 02**：自定义压缩扩展（07_实战代码_02_自定义压缩扩展.md）
- **实战代码 03**：压缩监控与调试（07_实战代码_03_压缩监控与调试.md）

---

**版本：** v1.0
**最后更新：** 2026-02-20
**维护者：** Claude Code
