# 类型系统与泛型 - 一句话总结

**类型系统是 Python 通过类型注解（如 `def func(x: int) -> str`）实现编译时类型检查的机制，泛型（TypeVar + Generic）让函数和类在保持类型安全的同时支持多种类型，LangChain 的 Runnable[Input, Output] 正是利用泛型实现了类型安全的组件组合，让 LCEL 管道在编写时就能发现类型错误，避免运行时崩溃，是构建可靠 AI Agent 的基础设施。**
