# 实战代码 06：项目配置实战

> **实战目标**：掌握完整的项目级配置，实现团队协作、多环境管理和最佳实践

---

## 一、完整项目配置示例

### 1.1 前端项目配置

```bash
#!/bin/bash
# setup-frontend-project.sh - 配置前端项目

PROJECT_NAME="my-frontend-app"
mkdir -p "$PROJECT_NAME"
cd "$PROJECT_NAME"

# 1. 初始化项目
npm init -y
npm install --save-dev typescript @types/node

# 2. 创建 .pi 目录
mkdir -p .pi/{skills,prompts,extensions}

# 3. 创建 settings.json
cat > .pi/settings.json << 'EOF'
{
  "provider": "anthropic",
  "model": "claude-sonnet-4",
  "thinkingLevel": "normal",
  "scopedModels": [
    "claude-haiku-4",
    "claude-sonnet-4",
    "gpt-4o"
  ],
  "compaction": {
    "enabled": true,
    "threshold": 0.8
  }
}
EOF

# 4. 创建 AGENTS.md
cat > AGENTS.md << 'EOF'
# 前端项目上下文

## 技术栈
- React 18 + TypeScript
- Vite 5
- Tailwind CSS
- Zustand (状态管理)

## 编码规范
- 使用函数组件和 Hooks
- Props 使用 interface 定义
- 组件文件使用 .tsx 扩展名
- 样式使用 Tailwind CSS

## 常用命令
```bash
npm run dev    # 开发服务器
npm test       # 运行测试
npm run build  # 构建生产版本
```

## 注意事项
- 所有组件必须有对应的测试
- 提交前运行 lint 和 test
- 使用语义化的 Git 提交消息
EOF

# 5. 创建 .gitignore
cat > .gitignore << 'EOF'
node_modules/
dist/
.pi/sessions/
.pi/auth.json
.env
.env.local
EOF

echo "✅ 前端项目配置完成"
```

### 1.2 后端项目配置

```bash
#!/bin/bash
# setup-backend-project.sh - 配置后端项目

PROJECT_NAME="my-backend-api"
mkdir -p "$PROJECT_NAME"
cd "$PROJECT_NAME"

# 1. 初始化项目
npm init -y
npm install --save-dev typescript @types/node

# 2. 创建 .pi/settings.json
mkdir -p .pi
cat > .pi/settings.json << 'EOF'
{
  "provider": "anthropic",
  "model": "claude-opus-4",
  "thinkingLevel": "extended",
  "scopedModels": [
    "claude-opus-4",
    "claude-sonnet-4"
  ]
}
EOF

# 3. 创建 AGENTS.md
cat > AGENTS.md << 'EOF'
# 后端 API 项目

## 技术栈
- Node.js + Express
- TypeScript
- Prisma ORM
- PostgreSQL

## 编码规范
- 使用 async/await
- 错误处理使用中间件
- API 遵循 REST 规范
- 所有端点需要认证

## 数据库
```bash
npx prisma migrate dev  # 运行迁移
npx prisma studio       # 打开数据库 GUI
```

## 安全要求
- 所有输入必须验证
- 使用参数化查询防止 SQL 注入
- 敏感数据必须加密
EOF

echo "✅ 后端项目配置完成"
```

---

## 二、团队协作配置

### 2.1 团队配置模板

```json
// .pi/settings.json - 团队共享配置
{
  "provider": "anthropic",
  "model": "claude-sonnet-4",
  "thinkingLevel": "normal",
  "scopedModels": [
    "claude-haiku-4",
    "claude-sonnet-4",
    "claude-opus-4"
  ],
  "compaction": {
    "enabled": true,
    "strategy": "auto",
    "threshold": 0.8,
    "keepRecent": 10
  }
}
```

### 2.2 团队 AGENTS.md

```markdown
<!-- AGENTS.md - 团队协作规范 -->
# 团队项目规范

## Pi 使用指南

### 模型选择
- **日常开发**: claude-sonnet-4
- **简单任务**: claude-haiku-4
- **复杂任务**: claude-opus-4

### 成本控制
- 默认使用 Sonnet 4
- 每日成本限制: $5
- 月度预算: $100

### 提交规范
- 使用 Pi 生成的代码必须经过 Code Review
- 提交前运行完整测试套件
- 提交消息格式: `type(scope): description`

## 项目结构
```
src/
├── components/  # React 组件
├── hooks/       # 自定义 Hooks
├── utils/       # 工具函数
└── types/       # TypeScript 类型
```

## 开发流程
1. 创建 feature 分支
2. 使用 Pi 辅助开发
3. 运行测试和 lint
4. 提交 PR
5. Code Review
6. 合并到 main
```

### 2.3 团队配置脚本

```bash
#!/bin/bash
# setup-team-config.sh - 配置团队共享设置

# 1. 创建团队配置目录
mkdir -p .pi

# 2. 复制团队配置模板
cat > .pi/settings.json << 'EOF'
{
  "provider": "anthropic",
  "model": "claude-sonnet-4",
  "scopedModels": ["claude-haiku-4", "claude-sonnet-4", "claude-opus-4"]
}
EOF

# 3. 创建团队 AGENTS.md
cat > AGENTS.md << 'EOF'
# 团队项目规范
[团队规范内容]
EOF

# 4. 添加到 Git
git add .pi/settings.json AGENTS.md
git commit -m "chore: add Pi team configuration"

# 5. 创建 README
cat >> README.md << 'EOF'

## Pi Coding Agent 配置

本项目使用 Pi Coding Agent 进行开发。

### 安装
```bash
npm install -g @mariozechner/pi-coding-agent
```

### 配置
项目配置已包含在 `.pi/settings.json` 和 `AGENTS.md` 中。

### 使用
```bash
cd project
pi
```
EOF

echo "✅ 团队配置完成"
```

---

## 三、多环境配置

### 3.1 环境配置脚本

```bash
#!/bin/bash
# setup-multi-env.sh - 配置多环境

# 创建环境配置文件
cat > .pi/settings.development.json << 'EOF'
{
  "provider": "ollama",
  "model": "llama3.1:8b",
  "scopedModels": ["llama3.1:8b", "qwen2.5-coder:7b"]
}
EOF

cat > .pi/settings.staging.json << 'EOF'
{
  "provider": "anthropic",
  "model": "claude-sonnet-4",
  "scopedModels": ["claude-sonnet-4", "claude-haiku-4"]
}
EOF

cat > .pi/settings.production.json << 'EOF'
{
  "provider": "anthropic",
  "model": "claude-opus-4",
  "scopedModels": ["claude-opus-4", "claude-sonnet-4"]
}
EOF

# 创建环境切换脚本
cat > switch-env.sh << 'EOF'
#!/bin/bash
ENV=$1

if [ -z "$ENV" ]; then
    echo "用法: ./switch-env.sh [development|staging|production]"
    exit 1
fi

if [ ! -f ".pi/settings.$ENV.json" ]; then
    echo "❌ 环境配置不存在: $ENV"
    exit 1
fi

cp ".pi/settings.$ENV.json" ".pi/settings.json"
echo "✅ 已切换到 $ENV 环境"
EOF

chmod +x switch-env.sh

echo "✅ 多环境配置完成"
```

### 3.2 环境切换使用

```bash
#!/bin/bash
# use-multi-env.sh - 使用多环境配置

# 开发环境（使用本地模型）
./switch-env.sh development
pi

# 测试环境（使用 Sonnet）
./switch-env.sh staging
pi

# 生产环境（使用 Opus）
./switch-env.sh production
pi
```

---

## 四、Skills 配置

### 4.1 创建项目 Skill

```bash
#!/bin/bash
# create-project-skill.sh - 创建项目专属 Skill

mkdir -p .pi/skills/deploy

cat > .pi/skills/deploy/SKILL.md << 'EOF'
---
name: deploy
description: Deploy the application to production
---

# Deploy Skill

Use this skill when the user asks to deploy the application.

## Prerequisites
- All tests must pass
- Code must be reviewed
- Staging deployment successful

## Steps

1. Run tests
```bash
npm test
```

2. Build production bundle
```bash
npm run build
```

3. Deploy to production
```bash
npm run deploy:prod
```

4. Verify deployment
```bash
curl https://api.example.com/health
```

## Rollback
If deployment fails:
```bash
npm run rollback
```
EOF

echo "✅ Deploy Skill 已创建"
```

### 4.2 创建测试 Skill

```bash
#!/bin/bash
# create-test-skill.sh - 创建测试 Skill

mkdir -p .pi/skills/test

cat > .pi/skills/test/SKILL.md << 'EOF'
---
name: test
description: Run comprehensive tests
---

# Test Skill

Run all tests for the project.

## Test Types

1. Unit Tests
```bash
npm run test:unit
```

2. Integration Tests
```bash
npm run test:integration
```

3. E2E Tests
```bash
npm run test:e2e
```

## Coverage
```bash
npm run test:coverage
```

Minimum coverage: 80%
EOF

echo "✅ Test Skill 已创建"
```

---

## 五、Prompt Templates 配置

### 5.1 创建组件模板

```bash
#!/bin/bash
# create-component-template.sh - 创建组件模板

mkdir -p .pi/prompts

cat > .pi/prompts/component.md << 'EOF'
---
name: component
description: Create a new React component
---

Create a new React component with the following requirements:

**Component Name**: {{name}}
**Props**: {{props}}
**Functionality**: {{functionality}}

Requirements:
- Use TypeScript
- Use function component with Hooks
- Include PropTypes interface
- Add basic styling with Tailwind CSS
- Create corresponding test file
- Add JSDoc comments

File structure:
```
src/components/{{name}}/
├── {{name}}.tsx
├── {{name}}.test.tsx
└── index.ts
```
EOF

echo "✅ Component Template 已创建"
```

### 5.2 创建 API 模板

```bash
#!/bin/bash
# create-api-template.sh - 创建 API 模板

cat > .pi/prompts/api.md << 'EOF'
---
name: api
description: Create a new API endpoint
---

Create a new API endpoint:

**Endpoint**: {{method}} {{path}}
**Description**: {{description}}
**Request Body**: {{request}}
**Response**: {{response}}

Requirements:
- Use Express Router
- Add input validation (Zod)
- Add error handling
- Add authentication middleware
- Add rate limiting
- Write integration tests
- Add API documentation

Example:
```typescript
router.{{method}}('{{path}}',
  authenticate,
  validate(schema),
  rateLimit,
  async (req, res) => {
    // Implementation
  }
);
```
EOF

echo "✅ API Template 已创建"
```

---

## 六、完整项目示例

### 6.1 创建完整项目

```typescript
// create-complete-project.ts - 创建完整项目

import * as fs from 'fs';
import * as path from 'path';
import { execSync } from 'child_process';

interface ProjectConfig {
  name: string;
  type: 'frontend' | 'backend' | 'fullstack';
  provider: string;
  model: string;
}

function createProject(config: ProjectConfig): void {
  const projectDir = config.name;

  // 1. 创建项目目录
  fs.mkdirSync(projectDir, { recursive: true });
  process.chdir(projectDir);

  // 2. 初始化 npm
  execSync('npm init -y', { stdio: 'inherit' });

  // 3. 安装依赖
  const deps = config.type === 'frontend'
    ? 'react react-dom typescript @types/react @types/react-dom'
    : 'express typescript @types/node @types/express';

  execSync(`npm install --save-dev ${deps}`, { stdio: 'inherit' });

  // 4. 创建 .pi 目录
  fs.mkdirSync('.pi/skills', { recursive: true });
  fs.mkdirSync('.pi/prompts', { recursive: true });

  // 5. 创建 settings.json
  const settings = {
    provider: config.provider,
    model: config.model,
    scopedModels: [
      'claude-haiku-4',
      'claude-sonnet-4',
      'claude-opus-4'
    ]
  };

  fs.writeFileSync(
    '.pi/settings.json',
    JSON.stringify(settings, null, 2)
  );

  // 6. 创建 AGENTS.md
  const agentsContent = `# ${config.name}

## 项目类型
${config.type}

## 技术栈
${config.type === 'frontend' ? '- React + TypeScript' : '- Node.js + Express'}

## 编码规范
- 使用 TypeScript 严格模式
- 遵循 ESLint 规则
- 所有函数必须有类型注解

## 常用命令
\`\`\`bash
npm run dev
npm test
npm run build
\`\`\`
`;

  fs.writeFileSync('AGENTS.md', agentsContent);

  // 7. 创建 .gitignore
  const gitignore = `node_modules/
dist/
.pi/sessions/
.pi/auth.json
.env
`;

  fs.writeFileSync('.gitignore', gitignore);

  console.log(`✅ 项目 ${config.name} 创建完成`);
}

// 使用示例
const config: ProjectConfig = {
  name: 'my-app',
  type: 'frontend',
  provider: 'anthropic',
  model: 'claude-sonnet-4'
};

createProject(config);
```

### 6.2 项目验证脚本

```bash
#!/bin/bash
# validate-project-config.sh - 验证项目配置

echo "🔍 验证项目配置..."

# 1. 检查 .pi/settings.json
if [ -f .pi/settings.json ]; then
    echo "✅ settings.json 存在"

    # 验证 JSON 格式
    if jq empty .pi/settings.json 2>/dev/null; then
        echo "✅ JSON 格式正确"
    else
        echo "❌ JSON 格式错误"
    fi
else
    echo "❌ settings.json 不存在"
fi

# 2. 检查 AGENTS.md
if [ -f AGENTS.md ]; then
    echo "✅ AGENTS.md 存在"
else
    echo "⚠️  AGENTS.md 不存在"
fi

# 3. 检查 Skills
if [ -d .pi/skills ]; then
    skill_count=$(find .pi/skills -name "SKILL.md" | wc -l)
    echo "✅ Skills 目录存在 ($skill_count 个 Skills)"
else
    echo "⚠️  Skills 目录不存在"
fi

# 4. 检查 Prompts
if [ -d .pi/prompts ]; then
    prompt_count=$(find .pi/prompts -name "*.md" | wc -l)
    echo "✅ Prompts 目录存在 ($prompt_count 个 Templates)"
else
    echo "⚠️  Prompts 目录不存在"
fi

# 5. 检查 .gitignore
if [ -f .gitignore ]; then
    if grep -q ".pi/sessions/" .gitignore && grep -q ".pi/auth.json" .gitignore; then
        echo "✅ .gitignore 配置正确"
    else
        echo "⚠️  .gitignore 缺少 Pi 相关配置"
    fi
else
    echo "⚠️  .gitignore 不存在"
fi

echo ""
echo "✨ 验证完成"
```

---

## 七、最佳实践

### 7.1 配置管理最佳实践

```bash
#!/bin/bash
# best-practices.sh - 配置管理最佳实践

cat << 'EOF'
📚 Pi 项目配置最佳实践

1. 版本控制
   ✅ 提交 .pi/settings.json 到 Git
   ✅ 提交 AGENTS.md 到 Git
   ✅ 提交 Skills 和 Prompts 到 Git
   ❌ 不要提交 .pi/sessions/
   ❌ 不要提交 .pi/auth.json

2. 团队协作
   ✅ 使用统一的模型配置
   ✅ 文档化 Pi 使用规范
   ✅ 定期同步配置更新
   ✅ 设置成本预算限制

3. 环境管理
   ✅ 开发环境使用本地模型
   ✅ 测试环境使用中等模型
   ✅ 生产环境使用最强模型
   ✅ 使用环境变量管理 API Key

4. 安全性
   ✅ 使用密钥管理工具
   ✅ 定期轮换 API Key
   ✅ 设置文件权限 (600)
   ✅ 不要在代码中硬编码密钥

5. 成本控制
   ✅ 默认使用中等模型
   ✅ 启用 Compaction
   ✅ 监控每日成本
   ✅ 设置预算提醒
EOF
```

### 7.2 配置检查清单

```markdown
# Pi 项目配置检查清单

## 基础配置
- [ ] .pi/settings.json 已创建
- [ ] AGENTS.md 已创建
- [ ] .gitignore 已配置
- [ ] README.md 包含 Pi 使用说明

## 团队协作
- [ ] 团队成员已安装 Pi
- [ ] 统一的模型配置
- [ ] 成本预算已设置
- [ ] Code Review 流程已建立

## 安全性
- [ ] API Key 使用密钥管理工具
- [ ] auth.json 权限正确 (600)
- [ ] 敏感文件已加入 .gitignore
- [ ] 定期轮换 API Key

## 功能扩展
- [ ] 项目专属 Skills 已创建
- [ ] Prompt Templates 已创建
- [ ] 环境配置已完成
- [ ] 验证脚本已创建
```

---

## 八、总结

### 8.1 配置文件总结

| 文件 | 作用 | 提交到 Git |
|------|------|-----------|
| `.pi/settings.json` | 项目配置 | ✅ 是 |
| `AGENTS.md` | 项目上下文 | ✅ 是 |
| `.pi/skills/` | 项目 Skills | ✅ 是 |
| `.pi/prompts/` | Prompt Templates | ✅ 是 |
| `.pi/sessions/` | 会话历史 | ❌ 否 |
| `.pi/auth.json` | 认证信息 | ❌ 否 |

### 8.2 快速参考

```bash
# 创建项目配置
mkdir -p .pi/{skills,prompts}
cat > .pi/settings.json << 'EOF'
{"provider":"anthropic","model":"claude-sonnet-4"}
EOF

# 验证配置
jq empty .pi/settings.json

# 切换环境
cp .pi/settings.production.json .pi/settings.json

# 重新加载配置
pi
/reload
```

---

**参考资料:**
- [Pi Settings Documentation](https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/docs/settings.md)
- [Pi Skills Documentation](https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/docs/skills.md)

**文档版本:** v1.0 (2026-02-18)
