# 安全与确认流程 - 实战代码

> **知识点**: Sub-Agents 子代理实现 - 安全确认与信任边界
> **难度**: ⭐⭐⭐⭐⭐
> **前置知识**: Agent 发现与注册、用户交互设计

---

## 1. 场景描述

安全与确认流程是 Sub-Agents 系统的关键安全机制,负责在代理执行敏感操作前获取用户同意,建立信任边界,并提供透明的操作审计。这确保用户始终掌控代理的行为,特别是对于 Project-level 代理。

**典型应用场景**:
- Project-level 代理首次执行需要用户确认
- 高风险操作(删除文件、网络请求)需要明确同意
- 渐进式授权(从建议到自主执行)
- 操作审计与撤销

根据 [Anthropic 的实际研究](https://www.anthropic.com/research/measuring-agent-autonomy),经验用户的自动批准率从 20% 增长到 40%,但中断率也从 5% 增长到 9%,表明用户从逐步批准转向自主监控策略。[Smashing Magazine 的 UX 模式指南](https://www.smashingmagazine.com/2026/02/designing-agentic-ai-practical-ux-patterns)提出了 6 个核心模式,包括 Intent Preview(接受率 >85%)和 Autonomy Dial(渐进式授权)。

---

## 2. 核心概念

### 2.1 信任边界

| 代理来源 | 信任级别 | 默认行为 | 原因 |
|---------|---------|---------|------|
| User-level | 高 | 自动执行 | 用户主动安装 |
| Project-level | 中 | 需要确认 | 项目特定,可能未知 |

### 2.2 确认策略

根据 [Anthropic 研究](https://www.anthropic.com/research/measuring-agent-autonomy):

| 用户类型 | 自动批准率 | 中断率 | 监督策略 |
|---------|-----------|--------|---------|
| 新用户 | ~20% | 5% | 逐步批准 |
| 经验用户 | >40% | 9% | 自主监控 |

### 2.3 UX 模式

根据 [Smashing Magazine](https://www.smashingmagazine.com/2026/02/designing-agentic-ai-practical-ux-patterns):

1. **Intent Preview**: 操作前确认(接受率 >85%)
2. **Autonomy Dial**: 渐进式授权(4个级别)
3. **Action Audit**: 操作审计与撤销

---

## 3. 完整代码示例

### 3.1 安全确认管理器

```typescript
import { ExtensionAPI } from "@pi-mono/extension-api";
import { z } from "zod";
import * as readline from "readline";

// 操作风险级别
enum RiskLevel {
  LOW = "low",
  MEDIUM = "medium",
  HIGH = "high",
  CRITICAL = "critical",
}

// 自主级别
enum AutonomyLevel {
  SUGGEST = 1,      // 仅建议
  DRAFT = 2,        // 准备但不执行
  EXECUTE_NOTIFY = 3, // 执行并通知
  FULLY_AUTONOMOUS = 4, // 完全自主
}

interface ConfirmationRequest {
  agent: string;
  action: string;
  description: string;
  riskLevel: RiskLevel;
  source: "user" | "project";
  details?: Record<string, any>;
}

interface ConfirmationResponse {
  approved: boolean;
  rememberChoice?: boolean;
  autonomyLevel?: AutonomyLevel;
}

/**
 * 安全确认管理器
 *
 * 参考:
 * - https://www.anthropic.com/research/measuring-agent-autonomy (Anthropic 研究)
 * - https://www.smashingmagazine.com/2026/02/designing-agentic-ai-practical-ux-patterns (UX 模式)
 */
export class SecurityConfirmationManager {
  private approvedAgents: Set<string> = new Set();
  private autonomyLevels: Map<string, AutonomyLevel> = new Map();
  private auditLog: Array<{
    timestamp: Date;
    agent: string;
    action: string;
    approved: boolean;
    riskLevel: RiskLevel;
  }> = [];

  /**
   * 请求确认
   */
  async requestConfirmation(
    request: ConfirmationRequest
  ): Promise<ConfirmationResponse> {
    // User-level agents 默认信任
    if (request.source === "user") {
      this.logAction(request, true);
      return { approved: true };
    }

    // 检查是否已批准
    if (this.approvedAgents.has(request.agent)) {
      this.logAction(request, true);
      return { approved: true };
    }

    // 检查自主级别
    const autonomyLevel = this.autonomyLevels.get(request.agent) || AutonomyLevel.SUGGEST;

    if (this.shouldAutoApprove(request.riskLevel, autonomyLevel)) {
      this.logAction(request, true);
      return { approved: true };
    }

    // 显示 Intent Preview
    const response = await this.showIntentPreview(request);

    this.logAction(request, response.approved);

    if (response.approved && response.rememberChoice) {
      this.approvedAgents.add(request.agent);
    }

    if (response.autonomyLevel) {
      this.autonomyLevels.set(request.agent, response.autonomyLevel);
    }

    return response;
  }

  /**
   * 判断是否自动批准
   */
  private shouldAutoApprove(riskLevel: RiskLevel, autonomyLevel: AutonomyLevel): boolean {
    // Level 4: 完全自主
    if (autonomyLevel === AutonomyLevel.FULLY_AUTONOMOUS) {
      return riskLevel !== RiskLevel.CRITICAL;
    }

    // Level 3: 执行并通知
    if (autonomyLevel === AutonomyLevel.EXECUTE_NOTIFY) {
      return riskLevel === RiskLevel.LOW;
    }

    // Level 1-2: 需要确认
    return false;
  }

  /**
   * 显示 Intent Preview
   */
  private async showIntentPreview(
    request: ConfirmationRequest
  ): Promise<ConfirmationResponse> {
    console.log("\n" + "=".repeat(60));
    console.log("🔒 Security Confirmation Required");
    console.log("=".repeat(60));
    console.log(`\nAgent: ${request.agent} (${request.source}-level)`);
    console.log(`Action: ${request.action}`);
    console.log(`Risk Level: ${this.getRiskEmoji(request.riskLevel)} ${request.riskLevel.toUpperCase()}`);
    console.log(`\nDescription:\n${request.description}`);

    if (request.details) {
      console.log(`\nDetails:`);
      for (const [key, value] of Object.entries(request.details)) {
        console.log(`  ${key}: ${JSON.stringify(value)}`);
      }
    }

    console.log("\n" + "-".repeat(60));
    console.log("Options:");
    console.log("  [A] Approve this action");
    console.log("  [R] Remember my choice for this agent");
    console.log("  [D] Deny this action");
    console.log("  [S] Set autonomy level");
    console.log("-".repeat(60));

    const rl = readline.createInterface({
      input: process.stdin,
      output: process.stdout,
    });

    return new Promise((resolve) => {
      rl.question("\nYour choice: ", (answer) => {
        rl.close();

        const choice = answer.trim().toUpperCase();

        if (choice === "A") {
          resolve({ approved: true });
        } else if (choice === "R") {
          resolve({ approved: true, rememberChoice: true });
        } else if (choice === "S") {
          this.promptAutonomyLevel(request.agent).then((level) => {
            resolve({ approved: true, autonomyLevel: level });
          });
        } else {
          resolve({ approved: false });
        }
      });
    });
  }

  /**
   * 提示设置自主级别
   */
  private async promptAutonomyLevel(agent: string): Promise<AutonomyLevel> {
    console.log("\n" + "=".repeat(60));
    console.log("Set Autonomy Level for " + agent);
    console.log("=".repeat(60));
    console.log("  [1] Suggest - Only provide suggestions");
    console.log("  [2] Draft - Prepare actions but don't execute");
    console.log("  [3] Execute & Notify - Execute low-risk actions");
    console.log("  [4] Fully Autonomous - Execute all non-critical actions");
    console.log("-".repeat(60));

    const rl = readline.createInterface({
      input: process.stdin,
      output: process.stdout,
    });

    return new Promise((resolve) => {
      rl.question("\nAutonomy level (1-4): ", (answer) => {
        rl.close();
        const level = parseInt(answer.trim());
        resolve((level >= 1 && level <= 4) ? level : AutonomyLevel.SUGGEST);
      });
    });
  }

  /**
   * 获取风险表情符号
   */
  private getRiskEmoji(riskLevel: RiskLevel): string {
    switch (riskLevel) {
      case RiskLevel.LOW: return "🟢";
      case RiskLevel.MEDIUM: return "🟡";
      case RiskLevel.HIGH: return "🟠";
      case RiskLevel.CRITICAL: return "🔴";
    }
  }

  /**
   * 记录操作
   */
  private logAction(request: ConfirmationRequest, approved: boolean): void {
    this.auditLog.push({
      timestamp: new Date(),
      agent: request.agent,
      action: request.action,
      approved,
      riskLevel: request.riskLevel,
    });
  }

  /**
   * 获取审计日志
   */
  getAuditLog(): typeof this.auditLog {
    return [...this.auditLog];
  }

  /**
   * 撤销代理批准
   */
  revokeApproval(agent: string): void {
    this.approvedAgents.delete(agent);
    this.autonomyLevels.delete(agent);
  }

  /**
   * 清除所有批准
   */
  clearAllApprovals(): void {
    this.approvedAgents.clear();
    this.autonomyLevels.clear();
  }
}

/**
 * 安全包装器 - 包装代理执行
 */
export class SecureAgentWrapper {
  private confirmationManager: SecurityConfirmationManager;

  constructor(confirmationManager: SecurityConfirmationManager) {
    this.confirmationManager = confirmationManager;
  }

  /**
   * 安全执行代理
   */
  async executeSecurely(
    agent: { name: string; source: "user" | "project" },
    action: string,
    description: string,
    riskLevel: RiskLevel,
    executor: () => Promise<any>
  ): Promise<{ success: boolean; result?: any; error?: string }> {
    try {
      // 请求确认
      const confirmation = await this.confirmationManager.requestConfirmation({
        agent: agent.name,
        action,
        description,
        riskLevel,
        source: agent.source,
      });

      if (!confirmation.approved) {
        return {
          success: false,
          error: "User denied permission",
        };
      }

      // 执行操作
      const result = await executor();

      return {
        success: true,
        result,
      };
    } catch (error: any) {
      return {
        success: false,
        error: error.message,
      };
    }
  }
}

/**
 * Extension 注册函数
 */
export default function (pi: ExtensionAPI) {
  const confirmationManager = new SecurityConfirmationManager();
  const secureWrapper = new SecureAgentWrapper(confirmationManager);

  pi.registerTool({
    name: "request_agent_confirmation",
    description: "Request user confirmation for agent action",
    parameters: z.object({
      agent: z.string(),
      action: z.string(),
      description: z.string(),
      riskLevel: z.enum(["low", "medium", "high", "critical"]),
      source: z.enum(["user", "project"]),
    }),
    async execute(toolCallId, params, signal, onUpdate, ctx) {
      const response = await confirmationManager.requestConfirmation({
        agent: params.agent,
        action: params.action,
        description: params.description,
        riskLevel: params.riskLevel as RiskLevel,
        source: params.source,
      });

      return {
        approved: response.approved,
        rememberChoice: response.rememberChoice,
        autonomyLevel: response.autonomyLevel,
      };
    },
  });

  pi.registerTool({
    name: "get_audit_log",
    description: "Get security audit log",
    parameters: z.object({}),
    async execute(toolCallId, params, signal, onUpdate, ctx) {
      const log = confirmationManager.getAuditLog();

      return {
        entries: log.map(entry => ({
          timestamp: entry.timestamp.toISOString(),
          agent: entry.agent,
          action: entry.action,
          approved: entry.approved,
          riskLevel: entry.riskLevel,
        })),
        count: log.length,
      };
    },
  });

  pi.registerTool({
    name: "revoke_agent_approval",
    description: "Revoke approval for an agent",
    parameters: z.object({
      agent: z.string(),
    }),
    async execute(toolCallId, params, signal, onUpdate, ctx) {
      confirmationManager.revokeApproval(params.agent);

      return {
        success: true,
        message: `Approval revoked for ${params.agent}`,
      };
    },
  });
}
```

---

## 4. 代码解析

### 4.1 核心设计决策

**1. 分层信任模型**

```typescript
// User-level: 默认信任
if (request.source === "user") {
  return { approved: true };
}

// Project-level: 需要确认
if (!this.approvedAgents.has(request.agent)) {
  return await this.showIntentPreview(request);
}
```

符合 [Anthropic 研究](https://www.anthropic.com/research/measuring-agent-autonomy)的信任积累模式。

**2. 渐进式授权**

```typescript
enum AutonomyLevel {
  SUGGEST = 1,           // 仅建议
  DRAFT = 2,             // 准备但不执行
  EXECUTE_NOTIFY = 3,    // 执行并通知
  FULLY_AUTONOMOUS = 4,  // 完全自主
}
```

实现 [Smashing Magazine](https://www.smashingmagazine.com/2026/02/designing-agentic-ai-practical-ux-patterns) 的 Autonomy Dial 模式。

**3. 风险感知确认**

```typescript
private shouldAutoApprove(riskLevel: RiskLevel, autonomyLevel: AutonomyLevel): boolean {
  if (autonomyLevel === AutonomyLevel.FULLY_AUTONOMOUS) {
    return riskLevel !== RiskLevel.CRITICAL;
  }
  // ...
}
```

---

## 5. 实际案例分析

### 案例 1: Anthropic - 用户同意模式演进

**来源**: [Anthropic Research](https://www.anthropic.com/research/measuring-agent-autonomy)

**关键发现**:
- **新用户**: 20% 自动批准率,5% 中断率
- **经验用户**: 40% 自动批准率,9% 中断率
- **监督策略转变**: 从逐步批准到自主监控

**启示**:
- 默认需要确认,允许用户逐步启用自动批准
- 经验用户更倾向于监控 + 干预,而非逐步批准
- 信任是通过经验逐步建立的

### 案例 2: Smashing Magazine - Intent Preview 模式

**来源**: [Smashing Magazine](https://www.smashingmagazine.com/2026/02/designing-agentic-ai-practical-ux-patterns)

**Intent Preview 示例**:
```
Proposed Plan for Your Trip Disruption
1. Cancel Flight UA456 - Process refund
2. Rebook on Flight DL789 - 2:30 PM
3. Update Hotel - Notify late arrival
4. Email Itinerary - Send to you and Jane

[ Proceed ] [ Edit Plan ] [ Handle it Myself ]
```

**成功指标**:
- 接受率 >85%
- 覆盖频率 <10%
- 回忆准确性 >90%

### 案例 3: GitHub - 安全实现

**来源**: [ruvnet/claude-flow](https://github.com/ruvnet/claude-flow)

**安全特性**:
- 提示注入防护
- 输入验证
- 威胁检测 AIDefence
- 安全审计代理

---

## 6. 最佳实践

### 6.1 确认时机

```typescript
// ✅ 需要确认
- Project-level 代理首次执行
- 高风险操作(删除、网络请求)
- 财务交易
- 数据共享

// ❌ 不需要确认
- User-level 代理
- 低风险操作(读取文件)
- 已批准的代理
```

### 6.2 Intent Preview 设计

```typescript
// ✅ 好的 Intent Preview
- 清晰简洁的操作描述
- 顺序步骤列表
- 明确的用户选择(Proceed / Edit / Deny)
- 风险级别指示

// ❌ 不好的 Intent Preview
- 技术术语过多
- 缺少操作细节
- 只有"确定"按钮
- 没有风险提示
```

### 6.3 自主级别设置

```typescript
// 根据任务风险调整
const autonomyLevels = {
  "scout": AutonomyLevel.FULLY_AUTONOMOUS,  // 低风险
  "worker": AutonomyLevel.EXECUTE_NOTIFY,   // 中风险
  "deployer": AutonomyLevel.SUGGEST,        // 高风险
};
```

---

## 7. 常见问题

### Q1: 如何平衡安全性和用户体验?

**A**: 根据 [Anthropic 研究](https://www.anthropic.com/research/measuring-agent-autonomy):

- **新用户**: 默认需要确认,建立信任
- **经验用户**: 允许自动批准,但保持监控能力
- **关键操作**: 始终需要确认,无论用户经验

### Q2: 何时使用 Intent Preview vs Autonomy Dial?

**A**: 根据 [Smashing Magazine](https://www.smashingmagazine.com/2026/02/designing-agentic-ai-practical-ux-patterns):

- **Intent Preview**: 单次操作确认(接受率 >85%)
- **Autonomy Dial**: 长期授权设置(4个级别)

### Q3: 如何处理用户疲劳?

**A**:
- 记住用户选择(`rememberChoice`)
- 渐进式授权(从 Level 1 到 Level 4)
- 智能分组(相似操作批量确认)

---

## 8. 扩展阅读

### 核心资源

1. **[Anthropic Research](https://www.anthropic.com/research/measuring-agent-autonomy)**
   - 实际用户同意模式
   - 自动批准率和中断率数据

2. **[Smashing Magazine](https://www.smashingmagazine.com/2026/02/designing-agentic-ai-practical-ux-patterns)**
   - 6个核心UX模式
   - Intent Preview和Autonomy Dial

3. **[ruvnet/claude-flow](https://github.com/ruvnet/claude-flow)**
   - 企业级安全实现
   - 提示注入防护

4. **[Curity Blog](https://curity.io/blog/user-consent-best-practices-in-the-age-of-ai-agents)**
   - 用户同意最佳实践
   - OAuth 2.1 集成

### 相关知识点

- `03_核心概念_06_安全模型与Agent_Scope.md` - 安全模型详解
- `07_实战代码_05_Agent发现与注册.md` - Agent 发现机制
- `07_实战代码_08_错误处理与恢复.md` - 错误处理策略

---

**版本**: v1.0
**最后更新**: 2026-02-21
**作者**: Claude Code
**审核**: 基于 2025-2026 年最新实践
