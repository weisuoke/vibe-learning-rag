# Collection管理 - 最小可用

> 掌握以下内容，5分钟内就能开始使用 Milvus 2.6 的 Collection 管理

---

## 学习目标

掌握以下内容，你就能：
- ✅ 创建一个基本的 Collection
- ✅ 查看和管理 Collection
- ✅ 为 RAG 系统设计简单的 Schema
- ✅ 使用 Milvus 2.6 的 Dynamic Schema 特性

**这些知识足以：**
- 开始构建简单的向量检索应用
- 为文档问答系统创建 Collection
- 理解 Collection 的基本操作流程
- 为后续深入学习打下基础

---

## 4.1 连接到 Milvus

**概念：** 所有操作的前提是连接到 Milvus 实例

```python
from pymilvus import connections

# 连接到本地 Milvus 2.6
connections.connect(
    alias="default",           # 连接别名
    host="localhost",          # Milvus 服务地址
    port="19530"               # 默认端口
)

print("✅ 已连接到 Milvus 2.6")
```

**实际应用：**
- 开发环境：连接到本地 Docker 容器
- 生产环境：连接到远程 Milvus 集群
- 多实例：使用不同的 alias 管理多个连接

**注意事项：**
- 确保 Milvus 2.6 服务已启动
- 默认端口是 19530
- 连接失败会抛出异常，需要处理

---

## 4.2 定义 Schema（数据结构）

**概念：** Schema 定义了 Collection 中数据的结构（字段、类型、约束）

```python
from pymilvus import FieldSchema, CollectionSchema, DataType

# ===== 1. 定义字段 =====
fields = [
    # 主键字段（必需）
    FieldSchema(
        name="id",
        dtype=DataType.INT64,
        is_primary=True,
        auto_id=True              # 自动生成 ID
    ),

    # 文本字段（存储原始文本）
    FieldSchema(
        name="text",
        dtype=DataType.VARCHAR,
        max_length=512            # 最大长度 512 字符
    ),

    # 向量字段（核心，用于检索）
    FieldSchema(
        name="vector",
        dtype=DataType.FLOAT_VECTOR,
        dim=768                   # 向量维度（如 OpenAI text-embedding-3-small）
    ),

    # 元数据字段（可选）
    FieldSchema(
        name="source",
        dtype=DataType.VARCHAR,
        max_length=128
    )
]

# ===== 2. 创建 Schema =====
schema = CollectionSchema(
    fields=fields,
    description="Document collection for RAG"
)

print("✅ Schema 定义完成")
```

**实际应用：**
- **文档问答系统**：text（文档内容）+ vector（向量表示）+ source（文档来源）
- **图像检索系统**：image_url（图片链接）+ vector（图像向量）+ category（分类）
- **推荐系统**：user_id（用户ID）+ item_vector（物品向量）+ timestamp（时间戳）

**字段类型速查：**
| 类型 | 用途 | 示例 |
|------|------|------|
| `INT64` | 主键、ID | 用户ID、文档ID |
| `VARCHAR` | 文本 | 文档内容、标签 |
| `FLOAT_VECTOR` | 向量 | Embedding 向量 |
| `JSON` | 复杂元数据 | `{"author": "张三", "tags": ["AI", "ML"]}` |
| `ARRAY` | 数组 | 标签列表 `["AI", "ML", "DL"]` |

---

## 4.3 创建 Collection

**概念：** 使用 Schema 创建 Collection

```python
from pymilvus import Collection

# ===== 创建 Collection =====
collection_name = "my_documents"
collection = Collection(
    name=collection_name,
    schema=schema
)

print(f"✅ Collection '{collection_name}' 创建成功")

# ===== 查看 Collection 信息 =====
print(f"Collection 名称: {collection.name}")
print(f"字段数量: {len(collection.schema.fields)}")
print(f"描述: {collection.schema.description}")
```

**实际应用：**
- **单租户应用**：创建一个 Collection 存储所有数据
- **多租户应用**：为每个租户创建独立 Collection（Milvus 2.6 支持 100K collections）
- **多模态应用**：为不同模态（文本、图像、音频）创建不同 Collection

**常见问题：**
- **Q: Collection 名称有限制吗？**
  - A: 是的，只能包含字母、数字、下划线，长度 1-255 字符

- **Q: 创建后可以修改 Schema 吗？**
  - A: Milvus 2.6 支持 Dynamic Schema，可以动态添加字段（见 4.5）

---

## 4.4 管理 Collection（查看、删除）

**概念：** Collection 的基本管理操作

```python
from pymilvus import utility

# ===== 1. 检查 Collection 是否存在 =====
if utility.has_collection("my_documents"):
    print("✅ Collection 存在")
else:
    print("❌ Collection 不存在")

# ===== 2. 列出所有 Collection =====
collections = utility.list_collections()
print(f"所有 Collection: {collections}")

# ===== 3. 查看 Collection 详情 =====
collection = Collection("my_documents")
print(f"Collection 名称: {collection.name}")
print(f"记录数: {collection.num_entities}")

# 查看 Schema
for field in collection.schema.fields:
    print(f"  - {field.name}: {field.dtype}")

# ===== 4. 删除 Collection =====
# 注意：删除操作不可逆！
# utility.drop_collection("my_documents")
# print("✅ Collection 已删除")

# ===== 5. 重命名 Collection（Milvus 2.6 新特性）=====
# utility.rename_collection("old_name", "new_name")
# print("✅ Collection 已重命名")
```

**实际应用：**
- **开发调试**：频繁创建/删除 Collection 测试
- **生产环境**：定期检查 Collection 状态，监控数据量
- **多租户管理**：列出所有租户的 Collection，批量管理

**最佳实践：**
- 删除前先备份数据
- 使用 `has_collection()` 避免重复创建
- 定期清理不再使用的 Collection

---

## 4.5 Dynamic Schema（Milvus 2.6 新特性）

**概念：** 运行时动态添加字段，无需重建 Collection

```python
from pymilvus import Collection, FieldSchema, DataType

# ===== 1. 获取现有 Collection =====
collection = Collection("my_documents")

# ===== 2. 动态添加新字段 =====
# 场景：业务需求变化，需要添加 "category" 字段
new_field = FieldSchema(
    name="category",
    dtype=DataType.VARCHAR,
    max_length=64
)

collection.add_field(new_field)
print("✅ 新字段 'category' 添加成功")

# ===== 3. 验证新字段 =====
for field in collection.schema.fields:
    print(f"  - {field.name}: {field.dtype}")
```

**实际应用：**
- **敏捷开发**：快速响应需求变化，无需停机维护
- **A/B 测试**：动态添加实验字段，测试新功能
- **渐进式迁移**：逐步添加新字段，避免一次性大规模迁移

**限制：**
- ❌ 不能修改已有字段的类型
- ❌ 不能修改向量维度
- ❌ 不能删除字段（只能创建新 Collection）
- ✅ 可以添加新的标量字段
- ✅ 可以添加新的向量字段

---

## 完整示例：5分钟创建一个 RAG Collection

```python
"""
Milvus 2.6 Collection 管理 - 最小可用示例
场景：为文档问答系统创建 Collection
"""

from pymilvus import connections, FieldSchema, CollectionSchema, DataType, Collection, utility

# ===== 1. 连接到 Milvus =====
connections.connect(
    alias="default",
    host="localhost",
    port="19530"
)
print("✅ 步骤1: 已连接到 Milvus 2.6")

# ===== 2. 定义 Schema =====
fields = [
    FieldSchema(name="id", dtype=DataType.INT64, is_primary=True, auto_id=True),
    FieldSchema(name="text", dtype=DataType.VARCHAR, max_length=512),
    FieldSchema(name="vector", dtype=DataType.FLOAT_VECTOR, dim=768),
    FieldSchema(name="source", dtype=DataType.VARCHAR, max_length=128)
]
schema = CollectionSchema(fields, description="RAG document collection")
print("✅ 步骤2: Schema 定义完成")

# ===== 3. 创建 Collection =====
collection_name = "rag_documents"

# 检查是否已存在
if utility.has_collection(collection_name):
    print(f"⚠️  Collection '{collection_name}' 已存在，删除旧的")
    utility.drop_collection(collection_name)

collection = Collection(name=collection_name, schema=schema)
print(f"✅ 步骤3: Collection '{collection_name}' 创建成功")

# ===== 4. 查看 Collection 信息 =====
print(f"\n📊 Collection 信息:")
print(f"  - 名称: {collection.name}")
print(f"  - 描述: {collection.schema.description}")
print(f"  - 字段:")
for field in collection.schema.fields:
    print(f"    • {field.name}: {field.dtype}")

# ===== 5. 动态添加字段（可选）=====
# 场景：后续需要添加文档分类
new_field = FieldSchema(name="category", dtype=DataType.VARCHAR, max_length=64)
collection.add_field(new_field)
print(f"\n✅ 步骤4: 动态添加 'category' 字段")

# ===== 6. 验证最终 Schema =====
print(f"\n📋 最终 Schema:")
for field in collection.schema.fields:
    print(f"  - {field.name}: {field.dtype}")

print("\n🎉 完成！Collection 已准备就绪，可以开始插入数据和检索")
```

**运行输出：**
```
✅ 步骤1: 已连接到 Milvus 2.6
✅ 步骤2: Schema 定义完成
✅ 步骤3: Collection 'rag_documents' 创建成功

📊 Collection 信息:
  - 名称: rag_documents
  - 描述: RAG document collection
  - 字段:
    • id: DataType.INT64
    • text: DataType.VARCHAR
    • vector: DataType.FLOAT_VECTOR
    • source: DataType.VARCHAR

✅ 步骤4: 动态添加 'category' 字段

📋 最终 Schema:
  - id: DataType.INT64
  - text: DataType.VARCHAR
  - vector: DataType.FLOAT_VECTOR
  - source: DataType.VARCHAR
  - category: DataType.VARCHAR

🎉 完成！Collection 已准备就绪，可以开始插入数据和检索
```

---

## 快速参考卡

### 创建 Collection 的 3 个步骤
```python
# 1. 连接
connections.connect("default", host="localhost", port="19530")

# 2. 定义 Schema
fields = [
    FieldSchema(name="id", dtype=DataType.INT64, is_primary=True, auto_id=True),
    FieldSchema(name="vector", dtype=DataType.FLOAT_VECTOR, dim=768)
]
schema = CollectionSchema(fields)

# 3. 创建 Collection
collection = Collection("my_collection", schema)
```

### 常用操作速查
```python
# 检查存在
utility.has_collection("name")

# 列出所有
utility.list_collections()

# 查看详情
collection.describe()

# 删除
utility.drop_collection("name")

# 重命名（2.6）
utility.rename_collection("old", "new")

# 动态添加字段（2.6）
collection.add_field(FieldSchema(...))
```

---

## 学习检查清单

完成本节学习后，你应该能够：

- [ ] 连接到 Milvus 2.6 实例
- [ ] 定义包含主键、文本、向量字段的 Schema
- [ ] 创建一个 Collection
- [ ] 检查 Collection 是否存在
- [ ] 列出所有 Collection
- [ ] 查看 Collection 的详细信息
- [ ] 删除 Collection
- [ ] 使用 Dynamic Schema 动态添加字段
- [ ] 理解 Milvus 2.6 的 100K collections 能力

---

## 常见问题

### Q1: 向量维度必须固定吗？
**A:** 是的，同一个向量字段的维度必须一致。如果使用 OpenAI `text-embedding-3-small`（768维），所有向量都必须是 768 维。

### Q2: 可以有多个向量字段吗？
**A:** 可以！Milvus 2.6 支持多向量检索，一个 Collection 可以有多个向量字段（如文本向量 + 图像向量）。

### Q3: Dynamic Schema 有什么限制？
**A:**
- ✅ 可以添加新的标量字段
- ✅ 可以添加新的向量字段
- ❌ 不能修改已有字段的类型
- ❌ 不能删除字段

### Q4: 100K Collections 是什么意思？
**A:** Milvus 2.6 单个实例可以支持 10 万个 Collection，适合大规模多租户 SaaS 应用（每个用户一个 Collection）。

### Q5: Collection 创建后可以修改名称吗？
**A:** 可以！Milvus 2.6 新增了 `rename_collection()` 方法。

---

## 下一步学习

- **深入理解**：[03_核心概念_1_Schema定义](./03_核心概念_1_Schema定义.md) - 深入学习 Schema 设计
- **实战代码**：[07_实战代码_场景1_基础Collection创建](./07_实战代码_场景1_基础Collection创建.md) - 完整的 RAG 示例
- **类比理解**：[05_双重类比](./05_双重类比.md) - 通过类比加深理解
- **避免误区**：[06_反直觉点](./06_反直觉点.md) - 常见误区与正确理解

---

## 总结

**你已经掌握了 Collection 管理的核心 20%：**
1. ✅ 连接到 Milvus
2. ✅ 定义 Schema（主键 + 文本 + 向量）
3. ✅ 创建和管理 Collection
4. ✅ 使用 Dynamic Schema 动态添加字段
5. ✅ 理解 Milvus 2.6 的 100K collections 能力

**这些知识足以：**
- 开始构建简单的 RAG 应用
- 为文档问答系统创建数据结构
- 理解 Collection 的基本操作流程

**继续学习，你将掌握：**
- 高级 Schema 设计（多向量、JSON、ARRAY）
- Collection 生命周期管理
- 多租户架构设计
- 性能优化技巧

---

**返回导航：** [00_概览](./00_概览.md)
