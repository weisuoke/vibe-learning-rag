# 最小可用

## 目标

用最少的代码和配置，创建并使用一个可工作的 Skill，理解 Skills 技能包系统的核心机制。

---

## 最小可用示例

### 步骤 1：创建技能目录

```bash
# 创建全局技能目录（如果不存在）
mkdir -p ~/.pi/agent/skills/hello-world
```

### 步骤 2：创建 SKILL.md 文件

```bash
# 创建技能文件
cat > ~/.pi/agent/skills/hello-world/SKILL.md << 'EOF'
---
name: hello-world
description: A simple hello world skill that demonstrates the basic structure
---

You are a friendly assistant. When the user provides a name, greet them warmly and provide a fun fact about their name if possible.

If no name is provided, ask for their name first.

Example:
- Input: "Alice"
- Output: "Hello, Alice! 👋 Did you know that Alice means 'noble' in German? It's also the name of the famous character from 'Alice in Wonderland'!"
EOF
```

### 步骤 3：使用技能

```bash
# 启动 pi-mono（技能会自动加载）
pi

# 在对话中使用技能
/skill:hello-world Alice
```

### 预期输出

```
Hello, Alice! 👋 Did you know that Alice means 'noble' in German?
It's also the name of the famous character from 'Alice in Wonderland'!
```

---

## 核心要素解析

### 1. 文件结构

```
~/.pi/agent/skills/
└── hello-world/              # 技能目录（目录名必须与 name 匹配）
    └── SKILL.md              # 技能文件（必须命名为 SKILL.md）
```

**关键点：**
- 目录名 `hello-world` 必须与 frontmatter 中的 `name` 匹配
- 文件必须命名为 `SKILL.md`（大小写敏感）
- 可以放在全局目录 `~/.pi/agent/skills/` 或项目目录 `./.pi/skills/`

### 2. YAML Frontmatter

```yaml
---
name: hello-world                    # 必需：技能名称（小写字母、数字、连字符）
description: A simple hello world... # 必需：技能描述（最多 1024 字符）
---
```

**验证规则（来自 skills.ts:91-115）：**
```typescript
// 名称验证
- 必须与父目录名匹配
- 最多 64 字符
- 只能包含小写字母、数字、连字符
- 不能以连字符开头或结尾
- 不能包含连续连字符

// 描述验证
- 必需字段
- 最多 1024 字符
```

### 3. Markdown 正文

```markdown
You are a friendly assistant. When the user provides a name...
```

**关键点：**
- 使用 Markdown 格式
- 定义技能的行为逻辑
- 可以包含示例、说明、约束等
- 会被注入到 System Prompt 中

### 4. 调用方式

```bash
/skill:hello-world Alice
```

**调用机制（来自 agent-session.ts:883-907）：**
```typescript
// 1. 解析命令
/skill:hello-world Alice
       ↓
skillName = "hello-world"
args = "Alice"

// 2. 查找技能
const skill = resourceLoader.getSkills().skills.find(s => s.name === skillName);

// 3. 读取文件
const content = readFileSync(skill.filePath, 'utf-8');
const body = stripFrontmatter(content).trim();

// 4. 展开为 XML 块
<skill name="hello-world" location="~/.pi/agent/skills/hello-world/SKILL.md">
References are relative to ~/.pi/agent/skills/hello-world.

You are a friendly assistant. When the user provides a name...
</skill>

Alice
```

---

## 进阶：带参数的技能

### 创建代码审查技能

```bash
mkdir -p ~/.pi/agent/skills/code-review
cat > ~/.pi/agent/skills/code-review/SKILL.md << 'EOF'
---
name: code-review
description: Review code for quality, potential issues, and improvements
---

You are an expert code reviewer. Analyze the provided code and provide:

1. **Code Quality Assessment**
   - Readability and maintainability
   - Code style and conventions
   - Naming and structure

2. **Potential Issues**
   - Bugs and edge cases
   - Performance concerns
   - Security vulnerabilities

3. **Improvement Suggestions**
   - Refactoring opportunities
   - Best practices
   - Alternative approaches

Format your review as:
- ✅ Strengths
- ⚠️ Issues
- 💡 Suggestions

Be constructive and specific in your feedback.
EOF
```

### 使用技能

```bash
# 审查文件
/skill:code-review src/components/Button.tsx

# 审查代码片段
/skill:code-review
```typescript
function add(a, b) {
  return a + b;
}
```
```

---

## 进阶：项目级技能

### 创建项目特定技能

```bash
# 在项目根目录创建技能
mkdir -p .pi/skills/project-setup
cat > .pi/skills/project-setup/SKILL.md << 'EOF'
---
name: project-setup
description: Set up development environment for this project
---

You are a project setup assistant for this specific codebase.

When asked to set up the development environment:

1. Check if Node.js is installed (version 18+)
2. Check if dependencies are installed (`node_modules` exists)
3. If not, run `npm install`
4. Check if `.env` file exists
5. If not, copy `.env.example` to `.env`
6. Verify the setup by running `npm run dev`

Provide clear step-by-step instructions and handle errors gracefully.
EOF
```

### 使用项目技能

```bash
# 项目技能会覆盖同名的全局技能
/skill:project-setup
```

**优先级（来自 resource-loader.ts）：**
```
项目技能 (./.pi/skills/) > 全局技能 (~/.pi/agent/skills/)
```

---

## 验证技能加载

### 方法 1：启动时查看日志

```bash
pi --verbose
# 输出会显示加载的技能列表
```

### 方法 2：使用技能列表命令

```bash
# 在 pi 对话中
/skills
# 显示所有可用技能
```

### 方法 3：检查技能文件

```bash
# 查看全局技能
ls -la ~/.pi/agent/skills/

# 查看项目技能
ls -la .pi/skills/
```

---

## 常见问题

### Q1: 技能没有被加载？

**检查清单：**
1. 文件名是否为 `SKILL.md`（大小写敏感）
2. 目录名是否与 `name` 字段匹配
3. YAML frontmatter 格式是否正确
4. `name` 和 `description` 字段是否存在

### Q2: 技能调用没有效果？

**检查清单：**
1. 技能名称是否正确（`/skill:name`）
2. 技能是否已加载（使用 `/skills` 查看）
3. 是否有语法错误（查看错误日志）

### Q3: 项目技能和全局技能冲突？

**解决方案：**
- 项目技能优先级更高
- 如果要使用全局技能，删除或重命名项目技能

---

## 最小可用总结

**创建技能的最小步骤：**

1. 创建目录：`mkdir -p ~/.pi/agent/skills/my-skill`
2. 创建文件：`touch ~/.pi/agent/skills/my-skill/SKILL.md`
3. 添加内容：
   ```markdown
   ---
   name: my-skill
   description: My custom skill
   ---

   [技能逻辑]
   ```
4. 使用技能：`/skill:my-skill`

**核心机制：**
- 文件系统加载（启动时自动扫描）
- YAML frontmatter 元数据（name + description）
- Markdown 正文逻辑（注入到 System Prompt）
- 命令式调用（`/skill:name args`）

---

## 下一步

- 【核心概念】- 深入学习 SKILL.md 格式规范
- 【实战代码】- 创建更复杂的技能
- 【技能组合】- 学习技能之间的组合

---

**版本：** v1.0
**最后更新：** 2026-02-20
**维护者：** Claude Code
