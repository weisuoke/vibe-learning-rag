# 所有权与借用 —— 一句话总结

> **知识点**: 所有权与借用
> **层级**: Phase1_Rust速成基础
> **维度**: 一句话总结

---

**所有权是 Rust 在编译期通过"一值一主人、借用分读写、出域即释放"三条规则实现零成本内存安全与并发安全的核心机制，在 ZeroClaw 中体现为 `Arc<T>` 共享、`Box<dyn Trait>` 抽象、`Mutex<T>` 内部可变的工程模式。**
