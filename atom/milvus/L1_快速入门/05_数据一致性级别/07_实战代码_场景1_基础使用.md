# 实战代码 - 场景1：基础使用

> 完整可运行的代码示例，演示三种一致性级别的基本使用

---

## 场景描述

**目标：** 理解三种一致性级别的基本用法和行为差异

**环境要求：**
- Python 3.9+
- pymilvus 2.3+
- Milvus 2.3+ (Docker 或本地部署)

---

## 完整代码

```python
"""
Milvus 数据一致性级别 - 基础使用示例
演示：Strong、Bounded、Eventually 三种一致性级别的基本行为
"""

from pymilvus import (
    connections,
    Collection,
    CollectionSchema,
    FieldSchema,
    DataType,
    utility
)
import time
import numpy as np

# ===== 1. 连接 Milvus =====
print("=== 连接 Milvus ===")
connections.connect(
    alias="default",
    host="localhost",
    port="19530"
)
print("✅ 连接成功\n")

# ===== 2. 创建 Collection =====
print("=== 创建 Collection ===")

# 定义 Schema
collection_name = "consistency_demo"

# 删除已存在的 Collection
if utility.has_collection(collection_name):
    utility.drop_collection(collection_name)
    print(f"删除已存在的 Collection: {collection_name}")

# 定义字段
fields = [
    FieldSchema(name="id", dtype=DataType.INT64, is_primary=True, auto_id=False),
    FieldSchema(name="embedding", dtype=DataType.FLOAT_VECTOR, dim=128),
    FieldSchema(name="text", dtype=DataType.VARCHAR, max_length=500)
]

schema = CollectionSchema(fields=fields, description="一致性级别演示")
collection = Collection(name=collection_name, schema=schema)
print(f"✅ 创建 Collection: {collection_name}\n")

# ===== 3. 创建索引 =====
print("=== 创建索引 ===")
index_params = {
    "index_type": "IVF_FLAT",
    "metric_type": "L2",
    "params": {"nlist": 128}
}
collection.create_index(field_name="embedding", index_params=index_params)
print("✅ 索引创建完成\n")

# ===== 4. 加载 Collection =====
print("=== 加载 Collection ===")
collection.load()
print("✅ Collection 已加载到内存\n")

# ===== 5. 插入初始数据 =====
print("=== 插入初始数据 ===")
initial_data = [
    [1, 2, 3],  # ids
    [
        np.random.rand(128).tolist(),
        np.random.rand(128).tolist(),
        np.random.rand(128).tolist()
    ],  # embeddings
    ["初始文档1", "初始文档2", "初始文档3"]  # texts
]
collection.insert(initial_data)
collection.flush()
print("✅ 插入 3 条初始数据\n")

time.sleep(2)  # 等待数据同步

# ===== 6. 测试 Strong 一致性 =====
print("=" * 60)
print("测试 1: Strong 一致性（强一致性）")
print("=" * 60)

# 插入新数据
print("\n[1] 插入新数据 (id=100)")
new_data = [
    [100],
    [np.random.rand(128).tolist()],
    ["Strong 一致性测试文档"]
]
collection.insert(new_data)
collection.flush()
print("✅ 数据插入完成")

# 立即查询（Strong）
print("\n[2] 立即查询 (consistency_level='Strong')")
start_time = time.time()
results_strong = collection.query(
    expr="id == 100",
    output_fields=["id", "text"],
    consistency_level="Strong"
)
query_time_strong = (time.time() - start_time) * 1000

print(f"查询结果: {results_strong}")
print(f"查询延迟: {query_time_strong:.2f}ms")
print(f"✅ Strong 一致性保证立即可见")

# ===== 7. 测试 Bounded 一致性 =====
print("\n" + "=" * 60)
print("测试 2: Bounded 一致性（有界一致性）")
print("=" * 60)

# 插入新数据
print("\n[1] 插入新数据 (id=200)")
new_data = [
    [200],
    [np.random.rand(128).tolist()],
    ["Bounded 一致性测试文档"]
]
collection.insert(new_data)
collection.flush()
print("✅ 数据插入完成")

# 立即查询（Bounded，容忍 5 秒延迟）
print("\n[2] 立即查询 (consistency_level='Bounded', 容忍 5 秒)")
start_time = time.time()
results_bounded = collection.query(
    expr="id == 200",
    output_fields=["id", "text"],
    consistency_level="Bounded",
    guarantee_timestamp=int(time.time() - 5)  # 容忍 5 秒延迟
)
query_time_bounded = (time.time() - start_time) * 1000

print(f"查询结果: {results_bounded}")
print(f"查询延迟: {query_time_bounded:.2f}ms")
print(f"⚠️ Bounded 可能查到，也可能查不到（取决于同步速度）")

# ===== 8. 测试 Eventually 一致性 =====
print("\n" + "=" * 60)
print("测试 3: Eventually 一致性（最终一致性）")
print("=" * 60)

# 插入新数据
print("\n[1] 插入新数据 (id=300)")
new_data = [
    [300],
    [np.random.rand(128).tolist()],
    ["Eventually 一致性测试文档"]
]
collection.insert(new_data)
collection.flush()
print("✅ 数据插入完成")

# 立即查询（Eventually）
print("\n[2] 立即查询 (consistency_level='Eventually')")
start_time = time.time()
results_eventually = collection.query(
    expr="id == 300",
    output_fields=["id", "text"],
    consistency_level="Eventually"
)
query_time_eventually = (time.time() - start_time) * 1000

print(f"查询结果: {results_eventually}")
print(f"查询延迟: {query_time_eventually:.2f}ms")
print(f"⚠️ Eventually 很可能查不到（数据还在同步中）")

# 等待后再查询
print("\n[3] 5 秒后再查询 (consistency_level='Eventually')")
time.sleep(5)
results_eventually_after = collection.query(
    expr="id == 300",
    output_fields=["id", "text"],
    consistency_level="Eventually"
)
print(f"查询结果: {results_eventually_after}")
print(f"✅ 等待后能查到（最终一致）")

# ===== 9. 性能对比 =====
print("\n" + "=" * 60)
print("性能对比总结")
print("=" * 60)

print(f"\nStrong 查询延迟:     {query_time_strong:.2f}ms")
print(f"Bounded 查询延迟:    {query_time_bounded:.2f}ms")
print(f"Eventually 查询延迟: {query_time_eventually:.2f}ms")

print(f"\n性能提升:")
print(f"Bounded vs Strong:    {((query_time_strong - query_time_bounded) / query_time_strong * 100):.1f}% 更快")
print(f"Eventually vs Strong: {((query_time_strong - query_time_eventually) / query_time_strong * 100):.1f}% 更快")

# ===== 10. 清理资源 =====
print("\n" + "=" * 60)
print("清理资源")
print("=" * 60)

collection.release()
utility.drop_collection(collection_name)
connections.disconnect("default")
print("✅ 资源清理完成")
```

---

## 运行输出示例

```
=== 连接 Milvus ===
✅ 连接成功

=== 创建 Collection ===
✅ 创建 Collection: consistency_demo

=== 创建索引 ===
✅ 索引创建完成

=== 加载 Collection ===
✅ Collection 已加载到内存

=== 插入初始数据 ===
✅ 插入 3 条初始数据

============================================================
测试 1: Strong 一致性（强一致性）
============================================================

[1] 插入新数据 (id=100)
✅ 数据插入完成

[2] 立即查询 (consistency_level='Strong')
查询结果: [{'id': 100, 'text': 'Strong 一致性测试文档'}]
查询延迟: 152.34ms
✅ Strong 一致性保证立即可见

============================================================
测试 2: Bounded 一致性（有界一致性）
============================================================

[1] 插入新数据 (id=200)
✅ 数据插入完成

[2] 立即查询 (consistency_level='Bounded', 容忍 5 秒)
查询结果: [{'id': 200, 'text': 'Bounded 一致性测试文档'}]
查询延迟: 78.56ms
⚠️ Bounded 可能查到，也可能查不到（取决于同步速度）

============================================================
测试 3: Eventually 一致性（最终一致性）
============================================================

[1] 插入新数据 (id=300)
✅ 数据插入完成

[2] 立即查询 (consistency_level='Eventually')
查询结果: []
查询延迟: 38.12ms
⚠️ Eventually 很可能查不到（数据还在同步中）

[3] 5 秒后再查询 (consistency_level='Eventually')
查询结果: [{'id': 300, 'text': 'Eventually 一致性测试文档'}]
✅ 等待后能查到（最终一致）

============================================================
性能对比总结
============================================================

Strong 查询延迟:     152.34ms
Bounded 查询延迟:    78.56ms
Eventually 查询延迟: 38.12ms

性能提升:
Bounded vs Strong:    48.4% 更快
Eventually vs Strong: 75.0% 更快

============================================================
清理资源
============================================================
✅ 资源清理完成
```

---

## 关键要点

### 1. Strong 一致性
- ✅ 插入后立即可查询到
- ❌ 查询延迟最高（~150ms）
- 🎯 适用场景：实时问答、文档刚上传

**代码要点：**
```python
# 必须配合 flush() 使用
collection.insert(data)
collection.flush()  # 确保数据持久化

# 使用 Strong 查询
results = collection.query(
    expr="id == 100",
    consistency_level="Strong"  # 保证立即可见
)
```

---

### 2. Bounded 一致性
- ⚠️ 插入后可能立即查到，也可能查不到
- ✅ 查询延迟适中（~80ms）
- 🎯 适用场景：日常检索、一般查询

**代码要点：**
```python
import time

# 使用 Bounded 查询，设置时间窗口
results = collection.query(
    expr="id == 200",
    consistency_level="Bounded",
    guarantee_timestamp=int(time.time() - 5)  # 容忍 5 秒延迟
)
```

---

### 3. Eventually 一致性
- ❌ 插入后立即查询很可能查不到
- ✅ 查询延迟最低（~40ms）
- ✅ 等待后一定能查到（最终一致）
- 🎯 适用场景：批量分析、历史数据

**代码要点：**
```python
# 使用 Eventually 查询
results = collection.query(
    expr="id == 300",
    consistency_level="Eventually"  # 最高性能
)

# 如果立即查询为空，等待后再查
if not results:
    time.sleep(5)
    results = collection.query(
        expr="id == 300",
        consistency_level="Eventually"
    )
    # 等待后能查到
```

---

### 4. 性能差异
- Bounded 比 Strong 快 40-50%
- Eventually 比 Strong 快 70-80%

**性能对比表：**

| 一致性级别 | 延迟 | 相对 Strong | 适用场景 |
|-----------|------|------------|---------|
| Strong | ~150ms | 基准 | 20% 场景 |
| Bounded | ~80ms | -46% | 60% 场景 |
| Eventually | ~40ms | -73% | 20% 场景 |

---

## 常见问题

### Q1: 为什么 Bounded 有时能立即查到？
**A:** 取决于数据同步速度。如果同步很快（< 5秒），就能查到。

**示例：**
```python
# 如果 Milvus 集群负载低，同步速度快
# Bounded 可能立即查到

# 如果 Milvus 集群负载高，同步速度慢
# Bounded 可能查不到
```

---

### Q2: Eventually 一定会查到吗？
**A:** 是的，"最终一致"保证最终会同步完成，只是时间不确定。

**验证代码：**
```python
# 插入数据
collection.insert(data)
collection.flush()

# 立即查询（Eventually）- 可能为空
results = collection.query(..., consistency_level="Eventually")

# 等待后查询 - 一定能查到
time.sleep(10)
results = collection.query(..., consistency_level="Eventually")
# 保证能查到

# 使用 Strong 验证 - 证明数据未丢失
results = collection.query(..., consistency_level="Strong")
# 一定能查到
```

---

### Q3: 如何选择一致性级别？
**A:** 根据场景选择：

**决策树：**
```
用户刚上传文档，立即提问？
├─ 是 → Strong
└─ 否 → 继续判断
    │
    查询历史数据（很少更新）？
    ├─ 是 → Eventually
    └─ 否 → Bounded（默认选择）
```

---

### Q4: 为什么需要 flush()？
**A:** flush() 确保数据从客户端缓冲区写入到 Milvus 服务器。

**对比：**
```python
# ❌ 错误：没有 flush
collection.insert(data)
results = collection.query(..., consistency_level="Strong")
# 可能查不到（数据还在缓冲区）

# ✅ 正确：使用 flush
collection.insert(data)
collection.flush()  # 确保数据写入
results = collection.query(..., consistency_level="Strong")
# 一定能查到
```

---

### Q5: 三种级别的使用比例？
**A:** 推荐比例：

- **Strong**: 20%（文档刚上传、实时场景）
- **Bounded**: 60%（日常查询、推荐）
- **Eventually**: 20%（批量分析、历史数据）

---

## 实践建议

### 1. 默认使用 Bounded
```python
# 大多数场景使用 Bounded
def search(query_embedding: list):
    return collection.search(
        data=[query_embedding],
        anns_field="embedding",
        param={"metric_type": "COSINE", "params": {"nprobe": 10}},
        limit=10,
        consistency_level="Bounded"  # 默认选择
    )
```

---

### 2. 仅在必要时使用 Strong
```python
# 仅在文档刚上传时使用 Strong
def search_after_upload(query_embedding: list):
    return collection.search(
        data=[query_embedding],
        anns_field="embedding",
        param={"metric_type": "COSINE", "params": {"nprobe": 10}},
        limit=10,
        consistency_level="Strong"  # 仅必要时使用
    )
```

---

### 3. 批量场景使用 Eventually
```python
# 批量分析使用 Eventually
def batch_search(query_embeddings: list):
    results = []
    for query_embedding in query_embeddings:
        result = collection.search(
            data=[query_embedding],
            anns_field="embedding",
            param={"metric_type": "COSINE", "params": {"nprobe": 10}},
            limit=10,
            consistency_level="Eventually"  # 批量场景
        )
        results.append(result)
    return results
```

---

## 下一步

完成基础使用后，建议：

1. **运行代码**
   - 在本地 Milvus 环境运行
   - 观察三种级别的行为差异
   - 记录性能数据

2. **实践练习**
   - 修改时间窗口参数（Bounded）
   - 测试不同数据量的性能
   - 在自己的项目中应用

3. **深入学习**
   - 场景2：性能对比测试
   - 场景3：RAG 应用集成
   - 面试必问：深入原理
