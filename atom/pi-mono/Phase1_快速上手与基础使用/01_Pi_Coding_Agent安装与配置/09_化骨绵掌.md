# 化骨绵掌：10 个 2 分钟知识卡片

> 将 Pi Coding Agent 安装与配置拆解为 10 个独立的知识卡片，每个 2 分钟内可看完

---

## 卡片 1：Pi 是什么？

**一句话：** Pi Coding Agent 是一个极简的终端 AI 编码助手，通过 npm 全局安装，支持 15+ LLM Provider。

**核心特点：**
- 🎯 极简设计：只提供 4 个基础工具（read、write、edit、bash）
- 🔌 高度可扩展：通过 TypeScript Extensions 实现任何功能
- 🌐 多 Provider：支持 Anthropic、OpenAI、xAI、Groq 等 15+ Provider
- 📦 模块化：可作为 SDK 嵌入到自己的应用中

**与其他工具的区别：**

| 工具 | 定位 | 扩展性 |
|------|------|--------|
| Claude Code | 完整的开箱即用工具 | 有限 |
| Cursor | 完整的 IDE | 有限 |
| Pi | 极简框架 + 可扩展 | 完全可编程 |

**应用场景：**
- 日常编码助手
- 构建自定义 AI Agent
- 集成到 Slack Bot、Web UI
- 学习 AI Agent 架构

**来源：** [Pi-mono README](https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/README.md) - 2026-02-17

---

## 卡片 2：npm 全局安装

**一句话：** Pi 通过 npm 全局安装，安装后任何目录都能运行 `pi` 命令。

**安装命令：**

```bash
# 全局安装
npm install -g @mariozechner/pi-coding-agent

# 验证安装
pi --version
# 输出：0.52.12（或更新版本）

# 更新
npm update -g @mariozechner/pi-coding-agent

# 卸载
npm uninstall -g @mariozechner/pi-coding-agent
```

**环境要求：**
- Node.js 18+ （推荐 20+）
- npm（Node.js 自带）
- 操作系统：macOS、Linux、Windows（WSL）

**从源码运行（开发者）：**

```bash
git clone https://github.com/badlogic/pi-mono.git
cd pi-mono
npm install
npm run build
./pi-test.sh
```

**应用：** 全局安装后，可以在任何项目目录中运行 `pi`，无需每个项目单独安装。

**来源：** [NPM 包页面](https://www.npmjs.com/package/@mariozechner/pi-coding-agent) - v0.52.12

---

## 卡片 3：Provider 认证方式

**一句话：** Pi 支持两种认证方式：API 密钥（适合生产）和 OAuth 订阅（适合个人）。

**方式 1：API 密钥**

```bash
# 环境变量（临时）
export ANTHROPIC_API_KEY=sk-ant-...
pi

# auth.json（持久化）
mkdir -p ~/.pi/agent
cat > ~/.pi/agent/auth.json << 'EOF'
{
  "anthropic": {
    "type": "api_key",
    "key": "sk-ant-..."
  }
}
EOF
chmod 600 ~/.pi/agent/auth.json
```

**方式 2：OAuth 订阅**

```bash
pi
/login
# 选择 Provider：
# - Claude Pro/Max
# - ChatGPT Plus/Pro
# - GitHub Copilot
# - Google Gemini CLI
```

**对比：**

| 特性 | API 密钥 | OAuth 订阅 |
|------|---------|-----------|
| 获取方式 | 手动创建 | 浏览器授权 |
| 成本控制 | 精确控制 | 固定订阅费 |
| 适用场景 | 生产环境、CI/CD | 个人使用 |
| 管理复杂度 | 需要管理密钥 | 自动刷新 |

**应用：** 个人学习用 OAuth，生产环境用 API 密钥。

**来源：** [Providers 文档](https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/docs/providers.md) - 2026-02-17

---

## 卡片 4：支持的 15+ Provider

**一句话：** Pi 支持 15+ LLM Provider，涵盖订阅 OAuth 和 API 密钥两种认证方式。

**订阅 OAuth Provider（5 个）：**

| Provider | 认证方式 | 特点 |
|----------|---------|------|
| Claude Pro/Max | /login | 高质量代码生成 |
| ChatGPT Plus/Pro | /login | 快速原型开发 |
| GitHub Copilot | /login | 企业环境 |
| Google Gemini CLI | /login | 免费，需要 Google 账号 |
| Google Antigravity | /login | 免费，支持多模型 |

**API 密钥 Provider（11+ 个）：**

| Provider | 环境变量 | 特点 |
|----------|---------|------|
| Anthropic | ANTHROPIC_API_KEY | 长上下文 |
| OpenAI | OPENAI_API_KEY | 通用任务 |
| xAI (Grok) | XAI_API_KEY | 实时信息 |
| Groq | GROQ_API_KEY | 超快推理 |
| Cerebras | CEREBRAS_API_KEY | 高性能 |
| Mistral | MISTRAL_API_KEY | 欧洲模型 |
| DeepSeek | OPENAI_API_KEY | 低成本 |
| OpenRouter | OPENROUTER_API_KEY | 多模型聚合 |
| Azure OpenAI | AZURE_OPENAI_API_KEY | 企业级 |
| Amazon Bedrock | AWS_PROFILE | 云原生 |
| Google Vertex AI | GOOGLE_CLOUD_PROJECT | 企业级 |

**应用：** 根据任务选择合适的 Provider，如 Claude 用于复杂推理，Groq 用于快速响应。

**来源：** [Providers 文档](https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/docs/providers.md) - 2026-02-17

---

## 卡片 5：配置文件层级

**一句话：** Pi 使用分层配置系统，优先级为：CLI 参数 > 项目配置 > 全局配置 > 默认值。

**配置文件位置：**

```bash
# 全局配置（个人偏好）
~/.pi/agent/settings.json
~/.pi/agent/AGENTS.md

# 项目配置（项目特定）
.pi/settings.json
.pi/AGENTS.md
```

**配置示例：**

```json
// 全局配置：~/.pi/agent/settings.json
{
  "theme": "dark",
  "defaultProvider": "anthropic",
  "defaultModel": "claude-sonnet-4"
}

// 项目配置：.pi/settings.json
{
  "defaultModel": "claude-opus-4",  // 覆盖全局
  "compaction": {
    "keepRecentTokens": 50000
  }
}
```

**配置合并规则：**
- 嵌套对象会合并（不是完全覆盖）
- 数组会完全覆盖（不是合并）
- CLI 参数优先级最高

**应用：** 全局配置设置个人偏好，项目配置设置项目特定需求，提交项目配置到 Git 实现团队共享。

**来源：** [Settings 文档](https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/docs/settings.md) - 2026-02-17

---

## 卡片 6：auth.json 文件详解

**一句话：** auth.json 存储所有 Provider 的认证凭证，支持 API 密钥、OAuth token 和密钥管理工具。

**文件位置和权限：**

```bash
~/.pi/agent/auth.json
# 权限：600（只有用户可读写）
```

**文件结构：**

```json
{
  "anthropic": {
    "type": "api_key",
    "key": "sk-ant-..."
  },
  "openai": {
    "type": "api_key",
    "key": "sk-..."
  },
  "anthropic-oauth": {
    "type": "oauth",
    "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refresh_token": "...",
    "expires_at": 1234567890
  }
}
```

**高级用法：密钥管理工具**

```json
{
  "anthropic": {
    "type": "api_key",
    "key": "!op read 'op://vault/anthropic/api-key'"
    // 从 1Password 读取，不存储明文
  },
  "openai": {
    "type": "api_key",
    "key": "!security find-generic-password -ws 'openai'"
    // 从 macOS Keychain 读取
  }
}
```

**密钥解析规则：**
1. `!command` - 执行 shell 命令，使用输出（缓存到进程生命周期）
2. `VARIABLE_NAME` - 读取环境变量
3. `sk-ant-...` - 直接使用字面值

**应用：** 使用密钥管理工具（1Password、Keychain）存储敏感信息，避免明文存储。

**来源：** [Providers 文档](https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/docs/providers.md) - 2026-02-17

---

## 卡片 7：首次运行体验

**一句话：** Pi 首次运行会自动检测可用的 Provider，显示启动信息，然后进入交互界面。

**启动流程：**

```bash
# 1. 启动 Pi
pi

# 2. Pi 自动检测
# - 检查环境变量（ANTHROPIC_API_KEY、OPENAI_API_KEY 等）
# - 检查 auth.json 文件
# - 加载全局和项目配置
# - 加载 AGENTS.md 上下文文件

# 3. 显示启动信息
# - 快捷键提示
# - 加载的 AGENTS.md 文件
# - 加载的 Extensions、Skills、Prompt Templates
# - 当前工作目录

# 4. 进入交互界面
# - 编辑器（输入区域）
# - 消息区域（对话历史）
# - 页脚（工作目录、Session、Token 使用、成本、模型）
```

**界面布局：**

```
┌─────────────────────────────────────────┐
│ 启动信息（快捷键、加载的文件）            │
├─────────────────────────────────────────┤
│                                         │
│ 消息区域（对话历史）                      │
│                                         │
├─────────────────────────────────────────┤
│ 编辑器（输入区域）                        │
├─────────────────────────────────────────┤
│ 页脚（工作目录、Session、Token、模型）    │
└─────────────────────────────────────────┘
```

**基础交互：**

```bash
# 输入消息
You: 读取 package.json 文件

# Pi 调用 read 工具
[Tool: read]
File: package.json
Content: { "name": "my-project", ... }

# Pi 生成回复
Assistant: 这是一个 Node.js 项目...
```

**应用：** 首次运行时，Pi 会自动检测并使用可用的 Provider，无需手动配置。

**来源：** [Pi-mono README](https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/README.md) - 2026-02-17

---

## 卡片 8：四大基础工具

**一句话：** Pi 默认提供 4 个工具（read、write、edit、bash），LLM 会根据任务自动选择调用。

**工具详解：**

| 工具 | 作用 | 示例场景 |
|------|------|---------|
| **read** | 读取文件内容 | 查看代码、读取配置 |
| **write** | 创建或覆盖文件 | 生成新文件 |
| **edit** | 精确编辑文件（替换文本） | 修改函数、重命名变量 |
| **bash** | 执行 shell 命令 | 运行测试、安装依赖 |

**工具调用示例：**

```bash
# 用户请求
You: 创建一个 hello.ts 文件，输出 Hello Pi

# Pi 调用 write 工具
[Tool: write]
File: hello.ts
Content:
console.log('Hello Pi')

# 用户请求
You: 把 Pi 改成 World

# Pi 调用 edit 工具
[Tool: edit]
File: hello.ts
Old: 'Hello Pi'
New: 'Hello World'

# 用户请求
You: 运行这个文件

# Pi 调用 bash 工具
[Tool: bash]
Command: npx tsx hello.ts
Output: Hello World
```

**工具特点：**
- ✅ 自动选择：LLM 根据任务自动选择工具
- ✅ 可见输出：工具调用和结果显示在终端
- ✅ 可折叠：Ctrl+O 折叠/展开工具输出
- ✅ 可扩展：通过 Extensions 添加自定义工具

**应用：** 你只需要用自然语言描述任务，Pi 会自动选择合适的工具执行。

**来源：** [Pi-mono README](https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/README.md) - 2026-02-17

---

## 卡片 9：切换 Provider 和 Model

**一句话：** Pi 支持通过快捷键、命令或 CLI 参数切换 Provider 和 Model。

**切换方式：**

```bash
# 方式 1：快捷键（最快）
Ctrl+L  # 打开模型选择器
# 使用方向键选择，Enter 确认

# 方式 2：命令
/model  # 打开模型选择器
/model claude-opus-4  # 直接切换到指定模型

# 方式 3：CLI 参数（启动时指定）
pi --provider anthropic --model claude-opus-4
pi --provider openai --model gpt-4o
pi --provider xai --model grok-2-latest

# 方式 4：Scoped Models（Ctrl+P 循环）
/scoped-models  # 配置可循环的模型
Ctrl+P  # 向前循环
Shift+Ctrl+P  # 向后循环
```

**常用模型推荐：**

| 模型 | Provider | 特点 | 适用场景 |
|------|----------|------|---------|
| claude-sonnet-4 | Anthropic | 平衡性能和成本 | 日常开发 |
| claude-opus-4 | Anthropic | 最强推理能力 | 复杂任务 |
| gpt-4o | OpenAI | 快速响应 | 快速原型 |
| grok-2-latest | xAI | 实时信息 | 需要最新知识 |
| gemini-2.0-flash | Google | 免费、快速 | 学习测试 |
| llama-3.3-70b | Groq | 超快推理 | 简单任务 |

**配置默认模型：**

```json
// ~/.pi/agent/settings.json
{
  "defaultProvider": "anthropic",
  "defaultModel": "claude-sonnet-4",
  "enabledModels": [
    "claude-*",
    "gpt-4o",
    "grok-2-latest"
  ]
}
```

**应用：** 根据任务复杂度选择模型，简单任务用便宜的模型，复杂任务用强大的模型。

**来源：** [Pi-mono README](https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/README.md) - 2026-02-17

---

## 卡片 10：项目配置最佳实践

**一句话：** 为项目创建 .pi/ 目录，配置项目特定的设置和上下文，提交到 Git 实现团队共享。

**完整的项目配置：**

```bash
# 1. 创建 .pi 目录
mkdir .pi

# 2. 创建项目配置
cat > .pi/settings.json << 'EOF'
{
  "defaultProvider": "anthropic",
  "defaultModel": "claude-sonnet-4",
  "compaction": {
    "keepRecentTokens": 30000
  },
  "retry": {
    "enabled": true,
    "maxRetries": 3
  }
}
EOF

# 3. 创建项目上下文
cat > .pi/AGENTS.md << 'EOF'
# 项目说明

这是一个 TypeScript 项目，使用 npm 管理依赖。

## 技术栈

- TypeScript 5.0+
- Node.js 20+
- Jest（测试框架）
- ESLint + Prettier（代码规范）

## 代码规范

- 使用 camelCase 命名函数和变量
- 使用 PascalCase 命名类和接口
- 所有函数必须有 JSDoc 注释
- 提交前必须通过 `npm run lint` 和 `npm test`

## 常用命令

- `npm run build` - 构建项目
- `npm test` - 运行测试
- `npm run lint` - 代码检查
- `npm run format` - 格式化代码

## 项目结构

- `src/` - 源代码
- `tests/` - 测试文件
- `dist/` - 构建输出
EOF

# 4. 创建 .gitignore
cat > .pi/.gitignore << 'EOF'
# 不提交个人的 auth.json
auth.json

# 不提交 Session 文件
sessions/
EOF

# 5. 提交到 Git
git add .pi/
git commit -m "Add Pi configuration for team"
```

**团队协作价值：**

```bash
# 团队成员克隆项目后
git clone <repo>
cd <project>
pi

# 自动获得：
# ✅ 项目推荐的模型
# ✅ 项目的代码规范说明
# ✅ 项目的常用命令
# ✅ 统一的配置（避免每人手动配置）
```

**高级配置：Extensions 和 Skills**

```bash
# 添加项目特定的 Extension
.pi/extensions/deploy.ts  # 部署工具
.pi/extensions/database.ts  # 数据库工具

# 添加项目特定的 Skill
.pi/skills/review-pr/SKILL.md  # PR 审查技能
.pi/skills/generate-docs/SKILL.md  # 文档生成技能

# 在 settings.json 中引用
{
  "extensions": ["./extensions/deploy.ts"],
  "skills": ["./skills/review-pr"]
}
```

**应用：** 项目配置是团队协作的关键，确保所有成员使用统一的设置和上下文。

**来源：** [Settings 文档](https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/docs/settings.md) - 2026-02-17

---

## 知识卡片总结

| 卡片 | 主题 | 核心要点 |
|------|------|---------|
| 1 | Pi 是什么 | 极简框架 + 可扩展 |
| 2 | npm 全局安装 | 一行命令，全局可用 |
| 3 | Provider 认证 | API 密钥 vs OAuth |
| 4 | 15+ Provider | 多模型支持 |
| 5 | 配置层级 | 全局 + 项目 + CLI |
| 6 | auth.json | 凭证存储 + 密钥管理 |
| 7 | 首次运行 | 自动检测 + 交互界面 |
| 8 | 四大工具 | read/write/edit/bash |
| 9 | 切换模型 | 快捷键 + 命令 + CLI |
| 10 | 项目配置 | 团队协作最佳实践 |

---

## 学习路径建议

**快速上手（10 分钟）：**
- 卡片 1、2、3、8 → 安装并开始使用

**完整理解（30 分钟）：**
- 按顺序阅读所有 10 个卡片

**深入学习（2 小时）：**
- 阅读所有卡片 + 核心概念 + 实战代码

---

**版本：** v1.0
**最后更新：** 2026-02-17
**维护者：** Claude Code
**基于：** Pi Coding Agent v0.52.12
