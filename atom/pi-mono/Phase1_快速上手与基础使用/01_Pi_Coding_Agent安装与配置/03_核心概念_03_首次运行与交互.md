# 核心概念 03：首次运行与交互

> **知识点定位**：掌握 Pi Coding Agent 的首次运行流程、交互模式界面、编辑器功能和基础命令

---

## 一、首次运行体验

### 1.1 启动 Pi

安装完成后，首次运行 Pi：

```bash
# 方式 1：使用环境变量认证
export ANTHROPIC_API_KEY=sk-ant-...
pi

# 方式 2：使用 OAuth 认证
pi
# 然后输入 /login
```

### 1.2 首次启动界面

```
┌─────────────────────────────────────────────────────────────┐
│ Pi Coding Agent v0.52.12                                    │
│ Shortcuts: /hotkeys | Loaded: AGENTS.md                    │
│ Extensions: 0 | Skills: 0 | Prompts: 0                     │
└─────────────────────────────────────────────────────────────┘

Welcome to Pi! Type your message or use / for commands.

┌─────────────────────────────────────────────────────────────┐
│ > _                                                         │
└─────────────────────────────────────────────────────────────┘

~/project | new-session | 0 tokens | $0.00 | claude-opus-4
```

**界面组成（从上到下）：**

1. **启动头部**：版本信息、快捷键提示、加载的配置文件
2. **消息区域**：对话历史、工具调用、通知
3. **编辑器**：输入区域，边框颜色表示思考级别
4. **底部状态栏**：工作目录、会话名称、Token 使用量、成本、当前模型

> **来源**：[Pi Coding Agent README - Interactive Mode](https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/README.md#interactive-mode) (2026-02)

### 1.3 Provider 自动检测

Pi 会自动检测可用的 Provider：

```bash
# 如果设置了 ANTHROPIC_API_KEY
pi
# 自动使用 Anthropic，默认模型 claude-opus-4

# 如果设置了多个 API Key
export ANTHROPIC_API_KEY=sk-ant-...
export OPENAI_API_KEY=sk-proj-...
pi
# 优先使用 Anthropic（按字母顺序）
```

**Provider 优先级：**
1. CLI 参数 `--provider` 和 `--model`
2. 项目配置 `.pi/settings.json`
3. 全局配置 `~/.pi/agent/settings.json`
4. 环境变量（按字母顺序）

### 1.4 首次交互示例

```
> 你好，帮我创建一个 TypeScript 项目

🤖 Assistant: 我来帮你创建一个 TypeScript 项目。

🔧 Tool: bash
$ mkdir my-project && cd my-project

🔧 Tool: bash
$ npm init -y

🔧 Tool: write
📝 Writing tsconfig.json

🔧 Tool: bash
$ npm install --save-dev typescript @types/node

✅ 项目创建完成！已安装 TypeScript 和类型定义。
```

**工具调用流程：**
1. Pi 理解用户意图
2. 调用 `bash` 工具创建目录
3. 调用 `write` 工具创建配置文件
4. 调用 `bash` 工具安装依赖
5. 返回结果给用户

---

## 二、交互模式界面详解

### 2.1 界面布局

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 启动头部 (Startup Header)                                │
│ Pi v0.52.12 | /hotkeys | AGENTS.md | Extensions: 2         │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 2. 消息区域 (Messages Area)                                 │
│                                                             │
│ User: 帮我创建一个函数                                       │
│                                                             │
│ Assistant: 我来创建一个函数。                                │
│                                                             │
│ 🔧 Tool: write                                              │
│ 📝 Writing src/utils.ts                                     │
│ ✅ File created                                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 3. 编辑器 (Editor)                                          │
│ > _                                                         │
└─────────────────────────────────────────────────────────────┘

4. 底部状态栏 (Footer)
~/project | session-name | 1.2K tokens | $0.05 | claude-opus-4
```

### 2.2 启动头部信息

**显示内容：**
- **版本号**：当前 Pi 版本
- **快捷键提示**：`/hotkeys` 查看所有快捷键
- **加载的文件**：`AGENTS.md`、`CLAUDE.md`
- **扩展统计**：Extensions、Skills、Prompts 数量

**示例：**
```
Pi Coding Agent v0.52.12
Shortcuts: /hotkeys | Loaded: AGENTS.md, .pi/AGENTS.md
Extensions: 2 | Skills: 5 | Prompts: 3
```

### 2.3 消息区域

**消息类型：**

1. **用户消息**：
   ```
   User: 帮我修复这个 bug
   ```

2. **助手响应**：
   ```
   Assistant: 我来分析这个问题...
   ```

3. **工具调用**：
   ```
   🔧 Tool: read
   📖 Reading src/app.ts (150 lines)
   ```

4. **工具结果**：
   ```
   ✅ File read successfully
   ```

5. **通知**：
   ```
   ℹ️ Session saved to ~/.pi/agent/sessions/...
   ```

6. **错误**：
   ```
   ❌ Error: File not found: src/missing.ts
   ```

### 2.4 编辑器边框颜色

编辑器边框颜色表示 **思考级别**（Thinking Level）：

| 颜色 | 思考级别 | 说明 |
|------|---------|------|
| 🟦 蓝色 | Normal | 标准模式，快速响应 |
| 🟨 黄色 | Extended | 扩展思考，更深入分析 |
| 🟥 红色 | Deep | 深度思考，复杂问题 |

**切换思考级别：**
- 快捷键：`Shift+Tab`
- 命令：`/settings` → Thinking Level

### 2.5 底部状态栏

**显示信息（从左到右）：**

1. **工作目录**：`~/project`
2. **会话名称**：`session-name` 或 `new-session`
3. **Token 使用量**：`1.2K tokens` (输入 + 输出)
4. **成本**：`$0.05` (累计成本)
5. **当前模型**：`claude-opus-4`

**实时更新：**
- Token 和成本在每次对话后更新
- 模型切换后立即显示新模型名称

---

## 三、编辑器功能详解

### 3.1 文件引用（@ 符号）

在编辑器中输入 `@` 触发文件模糊搜索：

```
> 帮我重构 @<输入文件名>

# 弹出文件选择器
┌─────────────────────────────────────┐
│ Search files:                       │
│ > app                               │
│                                     │
│ ✓ src/app.ts                        │
│   src/app.test.ts                   │
│   src/components/App.tsx            │
└─────────────────────────────────────┘
```

**使用场景：**
- 快速引用项目文件
- 避免手动输入完整路径
- 支持模糊匹配（如 `@app` 匹配 `src/app.ts`）

### 3.2 路径补全（Tab 键）

输入路径时按 `Tab` 自动补全：

```
> 读取 src/com<Tab>
# 自动补全为
> 读取 src/components/
```

### 3.3 多行输入

**macOS/Linux：**
- `Shift+Enter`：换行

**Windows Terminal：**
- `Ctrl+Enter`：换行

**示例：**
```
> 帮我创建一个函数，要求：<Shift+Enter>
1. 接受两个参数<Shift+Enter>
2. 返回它们的和<Shift+Enter>
3. 添加类型注解<Enter 发送>
```

### 3.4 图片粘贴

**方式 1：剪贴板粘贴**
```
Ctrl+V (或 Cmd+V)
```

**方式 2：拖拽**
```
将图片文件拖到终端窗口
```

**支持格式：**
- PNG、JPG、JPEG、GIF、WebP
- 自动上传并发送给模型

**使用场景：**
- 截图分析 UI 问题
- 设计稿转代码
- 错误截图调试

### 3.5 Bash 命令快捷执行

**语法：**
- `!command`：执行命令并发送输出给 LLM
- `!!command`：执行命令但不发送输出

**示例：**

```
> !git status
# 执行 git status，输出发送给 LLM

> !!npm install
# 执行 npm install，输出不发送给 LLM
```

**使用场景：**
- 快速查看文件内容：`!cat package.json`
- 检查 Git 状态：`!git diff`
- 运行测试：`!npm test`

### 3.6 标准编辑快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+A` | 移动到行首 |
| `Ctrl+E` | 移动到行尾 |
| `Ctrl+W` | 删除前一个单词 |
| `Ctrl+U` | 删除到行首 |
| `Ctrl+K` | 删除到行尾 |
| `Alt+Backspace` | 删除前一个单词 |
| `Ctrl+Z` | 撤销 |

> **来源**：[Pi Keybindings Documentation](https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/docs/keybindings.md) (2026-02)

---

## 四、基础命令详解

### 4.1 命令触发

在编辑器中输入 `/` 触发命令列表：

```
> /<输入命令名>

┌─────────────────────────────────────┐
│ Commands:                           │
│ /login                              │
│ /logout                             │
│ /model                              │
│ /settings                           │
│ /resume                             │
│ /new                                │
│ ...                                 │
└─────────────────────────────────────┘
```

### 4.2 认证命令

**`/login`**：OAuth 登录

```
> /login

┌─────────────────────────────────────┐
│ Select provider:                    │
│ ✓ Anthropic Claude Pro/Max         │
│   OpenAI ChatGPT Plus/Pro           │
│   GitHub Copilot                    │
│   Google Gemini CLI                 │
│   Google Antigravity                │
└─────────────────────────────────────┘

Opening browser for authentication...
✅ Logged in as user@example.com
```

**`/logout`**：清除 OAuth Token

```
> /logout
✅ Logged out from Anthropic
```

### 4.3 模型切换命令

**`/model`**：切换模型（或 `Ctrl+L`）

```
> /model

┌─────────────────────────────────────┐
│ Select model:                       │
│ Anthropic                           │
│ ✓ claude-opus-4                     │
│   claude-sonnet-4                   │
│   claude-haiku-4                    │
│                                     │
│ OpenAI                              │
│   gpt-4o                            │
│   o1                                │
│   o3-mini                           │
└─────────────────────────────────────┘
```

**`/scoped-models`**：设置快速切换模型

```
> /scoped-models

┌─────────────────────────────────────┐
│ Enable models for Ctrl+P cycling:  │
│ ☑ claude-opus-4                     │
│ ☑ claude-sonnet-4                   │
│ ☐ claude-haiku-4                    │
│ ☑ gpt-4o                            │
│ ☐ o1                                │
└─────────────────────────────────────┘

# 然后使用 Ctrl+P / Shift+Ctrl+P 快速切换
```

### 4.4 会话管理命令

**`/new`**：创建新会话

```
> /new
✅ New session created
```

**`/resume`**：恢复历史会话

```
> /resume

┌─────────────────────────────────────┐
│ Select session:                     │
│ ✓ 2026-02-18 10:30 - Fix bug       │
│   2026-02-17 15:20 - Add feature   │
│   2026-02-16 09:00 - Refactor      │
└─────────────────────────────────────┘
```

**`/name <name>`**：设置会话名称

```
> /name fix-auth-bug
✅ Session renamed to: fix-auth-bug
```

**`/session`**：查看会话信息

```
> /session

Session: fix-auth-bug
Path: ~/.pi/agent/sessions/project/fix-auth-bug.jsonl
Tokens: 15.2K (input: 8.1K, output: 7.1K)
Cost: $0.45
Model: claude-opus-4
Created: 2026-02-18 10:30
```

### 4.5 其他常用命令

**`/settings`**：打开设置界面

```
> /settings

┌─────────────────────────────────────┐
│ Settings:                           │
│ Thinking Level: Normal              │
│ Theme: default                      │
│ Message Delivery: one-at-a-time     │
│ Transport: auto                     │
└─────────────────────────────────────┘
```

**`/copy`**：复制最后一条助手消息

```
> /copy
✅ Copied to clipboard
```

**`/reload`**：重新加载扩展和配置

```
> /reload
✅ Reloaded: 2 extensions, 5 skills, 3 prompts
```

**`/hotkeys`**：显示所有快捷键

```
> /hotkeys

┌─────────────────────────────────────┐
│ Keyboard Shortcuts:                 │
│ Ctrl+C       Clear editor           │
│ Ctrl+L       Model selector         │
│ Ctrl+P       Cycle models forward   │
│ Shift+Tab    Cycle thinking level   │
│ Escape       Cancel/abort           │
│ ...                                 │
└─────────────────────────────────────┘
```

---

## 五、消息队列机制

### 5.1 消息队列概念

Pi 支持在 Agent 工作时提交新消息，消息会进入队列等待处理。

**两种消息类型：**

1. **Steering Message（引导消息）**：
   - 快捷键：`Enter`
   - 行为：在当前工具执行完成后立即发送，中断剩余工具调用

2. **Follow-up Message（跟进消息）**：
   - 快捷键：`Alt+Enter`
   - 行为：等待 Agent 完成所有工作后再发送

### 5.2 Steering Message 示例

```
# Agent 正在执行多个工具调用
🔧 Tool: read (正在执行)
🔧 Tool: edit (等待中)
🔧 Tool: bash (等待中)

# 用户按 Enter 发送 Steering Message
> 等等，不要修改那个文件<Enter>

# 结果：
✅ Tool: read 完成
❌ Tool: edit 被中断
❌ Tool: bash 被中断
🤖 Assistant: 好的，我不会修改文件。
```

### 5.3 Follow-up Message 示例

```
# Agent 正在执行工具调用
🔧 Tool: read
🔧 Tool: edit
🔧 Tool: bash

# 用户按 Alt+Enter 发送 Follow-up Message
> 完成后帮我运行测试<Alt+Enter>

# 结果：
✅ Tool: read 完成
✅ Tool: edit 完成
✅ Tool: bash 完成
🤖 Assistant: 修改完成。
# 然后自动处理 Follow-up Message
🤖 Assistant: 现在运行测试...
🔧 Tool: bash
$ npm test
```

### 5.4 消息队列管理

**取消队列消息：**
- 快捷键：`Escape`
- 行为：中止当前工作，恢复队列消息到编辑器

**检索队列消息：**
- 快捷键：`Alt+Up`
- 行为：将队列消息恢复到编辑器

### 5.5 消息传递模式配置

在 `/settings` 或 `settings.json` 中配置：

```json
{
  "steeringMode": "one-at-a-time",  // 或 "all"
  "followUpMode": "one-at-a-time"   // 或 "all"
}
```

**模式说明：**
- `"one-at-a-time"`（默认）：每次发送一条消息，等待响应
- `"all"`：一次性发送所有队列消息

---

## 六、键盘快捷键

### 6.1 常用快捷键

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `Ctrl+C` | 清空编辑器 | 按一次清空，按两次退出 |
| `Escape` | 取消/中止 | 按一次取消，按两次打开 `/tree` |
| `Ctrl+L` | 模型选择器 | 快速切换模型 |
| `Ctrl+P` | 向前循环模型 | 在 scoped models 中切换 |
| `Shift+Ctrl+P` | 向后循环模型 | 反向切换 |
| `Shift+Tab` | 切换思考级别 | Normal → Extended → Deep |
| `Ctrl+O` | 折叠/展开工具输出 | 隐藏冗长的工具结果 |
| `Ctrl+T` | 折叠/展开思考块 | 隐藏 Extended/Deep 思考内容 |

### 6.2 编辑器快捷键

| 快捷键 | 功能 |
|--------|------|
| `Shift+Enter` | 换行（macOS/Linux） |
| `Ctrl+Enter` | 换行（Windows Terminal） |
| `Ctrl+V` / `Cmd+V` | 粘贴图片 |
| `Tab` | 路径补全 |
| `@` | 文件引用 |

### 6.3 自定义快捷键

编辑 `~/.pi/agent/keybindings.json`：

```json
{
  "editor": {
    "submit": ["enter"],
    "newline": ["shift+enter"],
    "cancel": ["escape"],
    "clear": ["ctrl+c"]
  },
  "global": {
    "modelSelector": ["ctrl+l"],
    "cycleModelForward": ["ctrl+p"],
    "cycleModelBackward": ["shift+ctrl+p"],
    "cycleThinkingLevel": ["shift+tab"]
  }
}
```

> **来源**：[Pi Keybindings Documentation](https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/docs/keybindings.md) (2026-02)

---

## 七、实战技巧

### 7.1 快速开始新任务

```bash
# 方式 1：继续上次会话
pi -c

# 方式 2：创建新会话
pi
/new

# 方式 3：从命令行直接提问
pi "帮我创建一个 React 组件"
```

### 7.2 高效文件引用

```
# 使用 @ 引用多个文件
> 重构 @app.ts 和 @utils.ts，保持接口一致

# 使用 ! 命令查看文件
> !cat src/config.ts
然后帮我优化这个配置
```

### 7.3 多模型协作

```
# 使用 Opus 4 进行复杂重构
/model
选择 claude-opus-4
> 重构整个认证系统

# 切换到 Sonnet 4 进行快速修复
Ctrl+P
> 修复这个小 bug
```

### 7.4 会话管理最佳实践

```
# 为重要会话命名
/name refactor-auth-system

# 定期创建新会话避免上下文过长
/new

# 使用 /tree 回到历史节点
/tree
# 选择之前的某个状态继续
```

---

## 八、常见问题

### 8.1 编辑器无响应

**问题**：输入后按 Enter 无反应

**解决方案**：
```bash
# 检查是否在等待模型响应
# 查看编辑器边框颜色

# 如果卡住，按 Escape 取消
Escape

# 重启 pi
Ctrl+C (两次)
pi
```

### 8.2 快捷键冲突

**问题**：`Shift+Enter` 不换行

**解决方案**：
```bash
# Windows Terminal 用户使用 Ctrl+Enter
Ctrl+Enter

# 或修改 keybindings.json
{
  "editor": {
    "newline": ["ctrl+enter"]
  }
}
```

### 8.3 文件引用失败

**问题**：`@` 无法触发文件选择器

**解决方案**：
```bash
# 确保在项目目录中
cd /path/to/project
pi

# 或手动输入完整路径
> 读取 src/app.ts
```

---

## 九、总结

### 9.1 核心要点

1. **首次运行**：设置 API Key 或使用 `/login` OAuth 认证
2. **界面布局**：启动头部、消息区域、编辑器、状态栏
3. **编辑器功能**：文件引用（@）、路径补全（Tab）、多行输入、图片粘贴
4. **基础命令**：`/login`、`/model`、`/new`、`/resume`、`/settings`
5. **消息队列**：Steering（Enter）和 Follow-up（Alt+Enter）
6. **快捷键**：`Ctrl+L` 切换模型、`Ctrl+P` 循环模型、`Shift+Tab` 切换思考级别

### 9.2 最佳实践

- ✅ 使用 `@` 快速引用文件
- ✅ 使用 `!` 命令快速查看内容
- ✅ 为重要会话命名（`/name`）
- ✅ 使用 Scoped Models 快速切换常用模型
- ✅ 善用消息队列避免打断工作流

### 9.3 下一步

- 学习 **OAuth 订阅登录**（核心概念 04）
- 掌握 **环境变量配置**（核心概念 05）
- 了解 **项目级配置**（核心概念 06）

---

**参考资料：**
- [Pi Coding Agent README - Interactive Mode](https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/README.md#interactive-mode)
- [Pi Keybindings Documentation](https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/docs/keybindings.md)
- [Pi Commands Reference](https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/README.md#commands)

**文档版本**：v1.0 (2026-02-18)