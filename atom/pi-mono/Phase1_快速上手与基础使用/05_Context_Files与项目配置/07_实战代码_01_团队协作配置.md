# 实战代码：团队协作配置

> 完整的团队协作 Context Files 配置方案，包含 Git 管理、Onboarding 流程和最佳实践

---

## 一、场景描述

**团队背景：**
- 5 人前端团队
- TypeScript + React 项目
- 使用 pnpm + Vite
- 遵循 Git Flow 工作流
- 代码审查制度

**目标：**
- 统一团队的开发规范
- 新成员快速上手
- 减少重复沟通
- 提升 AI 辅助效率

---

## 二、完整配置方案

### 2.1 项目结构

```bash
my-team-project/
├── AGENTS.md                    # 团队共享规范（提交到 Git）
├── .pi/
│   └── settings.example.json   # 配置模板（提交到 Git）
├── .gitignore                   # 忽略个人配置
├── docs/
│   └── onboarding.md           # 新成员入职指南
├── src/
├── package.json
└── README.md
```

### 2.2 AGENTS.md（团队规范）

```markdown
# 项目规范 - 电商平台前端

## 项目概述
基于 TypeScript + React 的电商平台前端，支持商品浏览、购物车、订单管理等功能。

## 技术栈
- **语言**: TypeScript 5.3
- **框架**: React 18.2
- **构建工具**: Vite 5.0
- **包管理器**: pnpm 8.x
- **状态管理**: Zustand 4.x
- **样式方案**: Tailwind CSS 3.x
- **路由**: React Router v6
- **API 调用**: React Query (TanStack Query)
- **表单**: React Hook Form + Zod
- **测试**: Vitest + React Testing Library

## 目录结构
```
src/
├── features/           # 功能模块（按业务划分）
│   ├── products/       # 商品模块
│   │   ├── components/ # 商品相关组件
│   │   ├── hooks/      # 商品相关 Hooks
│   │   ├── api/        # 商品 API
│   │   └── types/      # 商品类型定义
│   ├── cart/           # 购物车模块
│   ├── orders/         # 订单模块
│   └── auth/           # 认证模块
├── components/         # 共享组件
│   ├── ui/             # UI 基础组件（Button、Input 等）
│   └── layout/         # 布局组件（Header、Footer 等）
├── hooks/              # 共享 Hooks
├── utils/              # 工具函数
├── api/                # API 客户端配置
├── types/              # 全局类型定义
├── styles/             # 全局样式
└── App.tsx             # 应用入口
```

## 开发规范

### 代码风格
- 使用 Prettier 格式化代码（配置见 .prettierrc）
- 遵循 ESLint 规则（配置见 .eslintrc.json）
- TypeScript 严格模式（strict: true）
- 所有组件必须有 TypeScript 类型
- 禁止使用 `any` 类型（除非有充分理由）

### 命名约定
- **组件**: PascalCase（例如：`UserProfile.tsx`）
- **文件**: kebab-case（例如：`user-profile.utils.ts`）
- **变量/函数**: camelCase（例如：`userName`, `getUserData`）
- **常量**: UPPER_SNAKE_CASE（例如：`API_BASE_URL`）
- **类型/接口**: PascalCase（例如：`User`, `ProductData`）
- **功能模块**: kebab-case（例如：`products/`, `cart/`）

### 组件规范
- 优先使用函数式组件
- 使用 React Hooks（不使用 class 组件）
- 组件文件结构：
  ```typescript
  // 1. 导入
  import React from 'react';
  import { useQuery } from '@tanstack/react-query';

  // 2. 类型定义
  interface UserProfileProps {
    userId: string;
  }

  // 3. 组件定义
  export function UserProfile({ userId }: UserProfileProps) {
    // Hooks
    const { data, isLoading } = useQuery(...);

    // 事件处理
    const handleClick = () => { ... };

    // 渲染
    return <div>...</div>;
  }
  ```

### 状态管理
- 全局状态使用 Zustand
- 服务端状态使用 React Query
- 表单状态使用 React Hook Form
- URL 状态使用 React Router

### API 调用
- 统一使用 `src/api/client.ts` 的封装方法
- 所有 API 调用使用 React Query
- API 端点定义在各功能模块的 `api/` 目录
- 示例：
  ```typescript
  // src/features/products/api/getProducts.ts
  import { apiClient } from '@/api/client';

  export async function getProducts() {
    return apiClient.get('/products');
  }
  ```

### 错误处理
- 使用 React Query 的错误处理机制
- 全局错误边界（ErrorBoundary）
- 用户友好的错误提示
- 错误日志记录到监控系统

### 测试要求
- 核心功能必须有单元测试
- 测试覆盖率目标：> 80%
- 测试文件命名：`*.test.ts` 或 `*.test.tsx`
- 测试文件放在被测试文件同目录

## Git 工作流

### 分支策略
- `main` - 生产环境分支（受保护）
- `develop` - 开发环境分支
- `feature/*` - 功能分支（从 develop 创建）
- `bugfix/*` - Bug 修复分支
- `hotfix/*` - 紧急修复分支（从 main 创建）

### 提交规范（Conventional Commits）
```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type 类型：**
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 重构（不是新功能也不是修复）
- `test`: 测试相关
- `chore`: 构建/工具相关

**示例：**
```
feat(products): add product search functionality

- Add search input component
- Implement search API integration
- Add search results display

Closes #123
```

### 工作流程
1. 从 `develop` 创建 feature 分支
   ```bash
   git checkout develop
   git pull origin develop
   git checkout -b feature/product-search
   ```

2. 开发并提交代码
   ```bash
   git add .
   git commit -m "feat(products): add search input"
   ```

3. 推送到远程并创建 PR
   ```bash
   git push origin feature/product-search
   # 在 GitHub 创建 Pull Request
   ```

4. Code Review 通过后合并到 develop

5. 定期从 develop 合并到 main 发布

## 常用命令

### 开发
```bash
pnpm dev              # 启动开发服务器（http://localhost:5173）
pnpm build            # 构建生产版本
pnpm preview          # 预览生产构建
```

### 代码质量
```bash
pnpm lint             # 运行 ESLint 检查
pnpm lint:fix         # 自动修复 ESLint 问题
pnpm format           # 运行 Prettier 格式化
pnpm type-check       # TypeScript 类型检查
```

### 测试
```bash
pnpm test             # 运行所有测试
pnpm test:watch       # 监听模式运行测试
pnpm test:coverage    # 生成测试覆盖率报告
```

### 依赖管理
```bash
pnpm install          # 安装依赖
pnpm add <package>    # 添加依赖
pnpm remove <package> # 移除依赖
```

## 注意事项

### 禁止操作
- ⚠️ **不要修改 `src/legacy/` 目录**（遗留代码，正在迁移中）
- ⚠️ **不要删除测试文件**（即使看起来无用）
- ⚠️ **不要直接修改 `package.json` 的依赖版本**（需要团队讨论）
- ⚠️ **不要提交 `.env` 文件到 Git**（包含敏感信息）
- ⚠️ **不要直接推送到 `main` 或 `develop` 分支**（必须通过 PR）

### 最佳实践
- ✅ 提交前运行 `pnpm lint` 和 `pnpm test`
- ✅ 保持 PR 小而专注（一个 PR 只做一件事）
- ✅ PR 描述清晰，包含截图或 GIF（如果是 UI 变更）
- ✅ 及时响应 Code Review 的反馈
- ✅ 定期从 develop 拉取最新代码

### API 调用规范
- 所有 API 调用必须使用 `src/api/client.ts`
- API 基础 URL 从环境变量读取（`VITE_API_BASE_URL`）
- 请求超时设置：10 秒
- 自动重试：失败后重试 2 次

### 环境变量
项目使用 `.env` 文件管理环境变量：
- `.env.development` - 开发环境
- `.env.production` - 生产环境
- `.env.local` - 本地覆盖（不提交到 Git）

**常用环境变量：**
```bash
VITE_API_BASE_URL=https://api.example.com
VITE_APP_NAME=电商平台
```

## 常见问题

### Q: 如何添加新的功能模块？
A: 在 `src/features/` 下创建新目录，参考现有模块的结构。

### Q: 如何添加新的 API 端点？
A: 在对应功能模块的 `api/` 目录下创建新文件，使用 `apiClient` 封装。

### Q: 如何添加新的路由？
A: 在 `src/App.tsx` 中添加路由配置。

### Q: 测试失败怎么办？
A: 先运行 `pnpm test:watch` 查看详细错误，修复后再提交。

## 相关文档
- [新成员入职指南](./docs/onboarding.md)
- [API 文档](./docs/api.md)
- [组件库文档](./docs/components.md)
- [部署指南](./docs/deployment.md)

---

**最后更新**: 2026-02-18
**维护者**: 前端团队
```

### 2.3 .pi/settings.example.json（配置模板）

```json
{
  "defaultModel": "claude-sonnet-4",
  "theme": "dark",
  "quietStartup": true,
  "collapseChangelog": true,
  "compaction": {
    "enabled": true,
    "reserveTokens": 50000,
    "keepRecentTokens": 10000
  },
  "skills": [
    "./.pi/skills/project"
  ]
}
```

### 2.4 .gitignore

```bash
# 依赖
node_modules/
.pnpm-store/

# 构建产物
dist/
build/

# 环境变量
.env.local
.env.*.local

# Pi Agent 个人配置
.pi/settings.json
.pi/SYSTEM.md
.pi/APPEND_SYSTEM.md

# IDE
.vscode/
.idea/

# 日志
*.log
npm-debug.log*

# 操作系统
.DS_Store
Thumbs.db
```

### 2.5 docs/onboarding.md（新成员入职指南）

```markdown
# 新成员入职指南

## 欢迎加入团队！

本指南帮助你快速上手项目开发。

## 第一步：环境准备

### 1. 安装必要工具
```bash
# 安装 Node.js（推荐使用 nvm）
nvm install 20
nvm use 20

# 安装 pnpm
npm install -g pnpm

# 安装 Pi Agent（可选，但强烈推荐）
npm install -g @anthropic-ai/pi
```

### 2. 克隆项目
```bash
git clone <repo-url>
cd my-team-project
```

### 3. 安装依赖
```bash
pnpm install
```

### 4. 配置环境变量
```bash
# 复制环境变量模板
cp .env.example .env.local

# 编辑 .env.local，填入你的配置
```

### 5. 配置 Pi Agent（可选）
```bash
# 复制配置模板
mkdir -p .pi
cp .pi/settings.example.json .pi/settings.json

# 根据个人偏好修改 .pi/settings.json
```

## 第二步：熟悉项目

### 1. 阅读项目规范
```bash
# 仔细阅读 AGENTS.md
cat AGENTS.md
```

### 2. 启动开发服务器
```bash
pnpm dev
# 访问 http://localhost:5173
```

### 3. 运行测试
```bash
pnpm test
```

## 第三步：开始开发

### 1. 创建你的第一个分支
```bash
git checkout develop
git pull origin develop
git checkout -b feature/your-first-task
```

### 2. 使用 Pi Agent 辅助开发
```bash
# 启动 Pi Agent
pi

# Pi Agent 会自动加载 AGENTS.md
# 现在你可以问它任何关于项目的问题
```

### 3. 提交代码
```bash
# 运行检查
pnpm lint
pnpm test

# 提交代码
git add .
git commit -m "feat: your first commit"
git push origin feature/your-first-task
```

### 4. 创建 Pull Request
- 在 GitHub 创建 PR
- 填写 PR 描述
- 等待 Code Review

## 常见问题

### Q: Pi Agent 是什么？
A: Pi Agent 是 Anthropic 的 AI 编程助手，可以帮助你理解代码、编写代码、调试问题等。

### Q: 为什么 Pi Agent 能理解我们的项目？
A: 因为项目根目录有 AGENTS.md 文件，Pi Agent 启动时会自动加载它，从而理解项目的规范和约定。

### Q: 我需要配置 Pi Agent 吗？
A: 不是必须的。AGENTS.md 是团队共享的，会自动加载。.pi/settings.json 是个人配置，可以根据个人偏好调整。

## 需要帮助？

- 技术问题：在团队 Slack 频道提问
- 项目规范：查看 AGENTS.md
- Pi Agent 使用：查看官方文档

---

祝你在团队工作愉快！🎉
```

---

## 三、团队协作流程

### 3.1 新成员 Onboarding

**第 1 天：环境准备**
```bash
# 1. 克隆项目
git clone <repo-url>
cd my-team-project

# 2. 安装依赖
pnpm install

# 3. 配置环境变量
cp .env.example .env.local

# 4. 启动项目
pnpm dev

# 5. 运行测试
pnpm test
```

**第 2 天：熟悉项目**
```bash
# 1. 阅读 AGENTS.md
cat AGENTS.md

# 2. 阅读 onboarding.md
cat docs/onboarding.md

# 3. 启动 Pi Agent
pi
# Pi Agent 会自动加载 AGENTS.md

# 4. 问 Pi Agent 问题
# "项目的目录结构是什么？"
# "如何添加新的功能模块？"
# "提交规范是什么？"
```

**第 3 天：开始开发**
```bash
# 1. 创建第一个分支
git checkout -b feature/my-first-task

# 2. 使用 Pi Agent 辅助开发
pi
# "帮我创建一个新的商品列表组件"

# 3. 提交代码
pnpm lint && pnpm test
git add .
git commit -m "feat(products): add product list component"
git push origin feature/my-first-task
```

### 3.2 日常开发流程

**1. 开始新任务**
```bash
# 拉取最新代码
git checkout develop
git pull origin develop

# 创建 feature 分支
git checkout -b feature/task-name

# 启动 Pi Agent
pi
```

**2. 开发过程**
```bash
# 使用 Pi Agent 辅助开发
# Pi Agent 会自动遵循 AGENTS.md 中的规范

# 定期提交
git add .
git commit -m "feat: implement feature"
```

**3. 提交前检查**
```bash
# 运行 lint
pnpm lint

# 运行测试
pnpm test

# 类型检查
pnpm type-check
```

**4. 创建 Pull Request**
```bash
# 推送到远程
git push origin feature/task-name

# 在 GitHub 创建 PR
# PR 描述模板：
# ## 变更内容
# - 添加了 XXX 功能
# - 修复了 XXX 问题
#
# ## 测试
# - [ ] 单元测试通过
# - [ ] 手动测试通过
#
# ## 截图
# （如果是 UI 变更）
```

**5. Code Review**
```bash
# 响应 Review 反馈
# 修改代码后重新推送
git add .
git commit -m "fix: address review comments"
git push origin feature/task-name
```

**6. 合并后清理**
```bash
# PR 合并后，删除本地分支
git checkout develop
git pull origin develop
git branch -d feature/task-name
```

### 3.3 规范更新流程

**场景：团队决定添加新的规范**

```bash
# 1. 修改 AGENTS.md
echo "- 新规范：使用 React Query 管理服务端状态" >> AGENTS.md

# 2. 提交并推送
git add AGENTS.md
git commit -m "docs: add React Query convention"
git push origin develop

# 3. 通知团队
# 在 Slack 发消息：
# "更新了 AGENTS.md，添加了 React Query 规范，请大家拉取最新代码"

# 4. 团队成员拉取更新
git pull origin develop

# 5. 重新加载 Pi Agent
pi
/reload
```

---

## 四、Git 管理策略

### 4.1 提交到 Git 的文件

```bash
# ✅ 提交（团队共享）
AGENTS.md                    # 团队规范
.pi/settings.example.json    # 配置模板
docs/onboarding.md           # 入职指南
.gitignore                   # Git 忽略规则

# ❌ 不提交（个人配置）
.pi/settings.json            # 个人配置
.pi/SYSTEM.md                # 个人 AI 行为
.pi/APPEND_SYSTEM.md         # 个人 AI 行为
.env.local                   # 本地环境变量
```

### 4.2 .gitignore 配置

```bash
# Pi Agent 个人配置
.pi/settings.json
.pi/SYSTEM.md
.pi/APPEND_SYSTEM.md

# 环境变量
.env.local
.env.*.local

# 其他个人文件
.vscode/
.idea/
```

### 4.3 配置模板管理

**提供配置模板供团队参考：**

```bash
# .pi/settings.example.json（提交到 Git）
{
  "defaultModel": "claude-sonnet-4",
  "theme": "dark",
  "compaction": {
    "enabled": true,
    "reserveTokens": 50000
  }
}

# 新成员使用模板
cp .pi/settings.example.json .pi/settings.json
# 然后根据个人偏好修改
```

---

## 五、实际效果

### 5.1 新成员 Onboarding 时间

**使用 Context Files 前：**
- 第 1 周：熟悉项目结构和规范
- 第 2 周：开始独立开发
- 第 3 周：熟悉团队工作流

**使用 Context Files 后：**
- 第 1 天：环境准备 + 熟悉项目
- 第 2 天：开始独立开发
- 第 3 天：熟悉团队工作流

**时间节省：70%**

### 5.2 重复沟通减少

**使用前：**
- "这个项目用什么技术栈？"
- "代码风格是什么？"
- "提交规范是什么？"
- "如何添加新功能？"

**使用后：**
- 所有问题都在 AGENTS.md 中
- Pi Agent 自动遵循规范
- 新成员自助查阅

**沟通成本降低：80%**

### 5.3 代码质量提升

**使用前：**
- 代码风格不统一
- 命名规范不一致
- 提交信息混乱

**使用后：**
- Pi Agent 自动遵循 AGENTS.md 规范
- 代码风格统一
- 提交信息规范

**Code Review 时间节省：50%**

---

## 六、常见问题

### Q1: 如何确保团队成员都使用 AGENTS.md？

**A:** 通过以下方式：
1. 在 onboarding 文档中强调
2. 在团队会议中宣传
3. 在 Code Review 中检查
4. 定期更新和维护 AGENTS.md

### Q2: 个人配置和团队配置冲突怎么办？

**A:** 遵循以下原则：
- AGENTS.md：只包含团队共识
- .pi/settings.json：个人偏好（不提交）
- 如果有冲突，团队共识优先

### Q3: 如何处理不同项目的配置？

**A:** 每个项目有自己的 AGENTS.md：
```bash
project-a/
└── AGENTS.md  # 项目 A 的规范

project-b/
└── AGENTS.md  # 项目 B 的规范
```

### Q4: AGENTS.md 太长怎么办？

**A:** 使用渐进式披露：
```bash
# 根目录：通用规范
/project/AGENTS.md

# 子目录：特定规范
/project/src/features/products/AGENTS.md
```

---

## 七、总结

**团队协作配置的核心要点：**

1. **AGENTS.md 提交到 Git** - 团队共享规范
2. **.pi/settings.json 不提交** - 个人配置
3. **提供配置模板** - .pi/settings.example.json
4. **完善 Onboarding 文档** - 帮助新成员快速上手
5. **定期更新维护** - 保持 AGENTS.md 的时效性

**效果：**
- Onboarding 时间减少 70%
- 重复沟通减少 80%
- Code Review 时间节省 50%
- 代码质量显著提升

**记住：** Context Files 是团队协作的"共同语言"，让 AI 和人类都能理解项目规范！
